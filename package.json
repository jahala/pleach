{
  "name": "pleach",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src test",
    "check": "bun run typecheck && bun run lint && bun run test"
  },
  "dependencies": {
    "zod": "^4.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/node": "^25.9.3",
    "bun-types": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
