{
  "goal": "A seam's surprise never costs the tree (D23) — docs/tend2/seam-keeps-tree.tend2.html",
  "source": "docs/tend2/seam-keeps-tree.tend2.html",
  "maxConcurrency": 2,
  "nodes": [
    {
      "id": "sk.reason",
      "worker": {
        "provider": "claude",
        "model": "claude-opus-5"
      },
      "work": {
        "test": "bun run check",
        "phases": [
          {
            "phase": "red",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c1)\n- (code) The umbel adapter's `wait` takes the reason from the JSON whatever the exit code: a wait that exits 124 with a timeout reason yields a `timeout` result, and the same for 122 provider-error, 123 idle, 125 dead and 126 input, each with its message; `WorkerSeamError` is thrown only when no reason parses, naming the exit code and the output · evidence: test/unit/umbel-wait-reasons.test.ts\n\n## How this node fits\nThe rule is the runner seam's: contracts/runner.md on jahala/plotplot, section 'A non-zero exit with a reason is a result' — cite it in the test's header. A RED TEST IS ALREADY DRAFTED and proven red against master for the right reason (five rows throw 'wait exited N' before the reason is read; three boundary cases pass): docs/dogfood/seam-keeps-tree/red/umbel-wait-reasons.test.ts.draft. In the RED phase copy it verbatim to the evidence path (cp), run it, confirm it fails as its header says, and change only what the code's real shapes force — say what in Tried. What it pins, for the implementation: a unit test with a scripted ExecFn: spawn + status succeed; send succeeds; `wait` returns {exitCode: N, output/stdout: '{\"reason\":\"<r>\",\"message\":\"<m>\"}\\n'} for each row (124 timeout, 122 provider-error, 123 idle, 125 dead, 126 input) → worker.wait() resolves with reason r and, where the adapter carries one, message m (extend gatherWorkerResult so provider-error, timeout and dead carry `message` too — the contract says every non-progress reason carries what was observed); exit 1 with 'umbel: tmux exploded' → rejects WorkerSeamError whose message holds 'exited 1' and the output; exit 0 with garbage → the existing unparseable error; the aborted-signal early answer is unchanged (test/integration/umbel-abort.test.ts stays green). Implementation: parse first; when stdout parses to an object with a string reason, that is the result whatever the exit code; otherwise throw as today, naming the exit code. Add 'provider-error' | 'file' | 'pattern' to WorkerResult's union in src/loop/deps.ts ONLY as far as typecheck needs here (the classifier is the third check's). MASTER'S CHECK IS RED ON THIS MACHINE before you touch anything (jahala/pleach#121): the installed umbel is rebuilt from its master, so test/integration/umbel-idle.test.ts (two tests) and the wedged-worker runPlan test over the umbel runner fail on this very defect and go green with your fix; and test/integration/umbel-abort.test.ts's sessionAlive helper reads `umbel status <name>`'s exit code as liveness, but since jahala/umbel#73 a killed session keeps a tombstone and status exits 0 with STATUS dead (exit 1 only after kill --purge) — make the helper read liveness (`umbel status <name> --json` or the STATUS column) without weakening what the test asserts. All four must be green when you finish; run them with the real binary, they are not skipped here. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: RED\nWrite the failing test FIRST at test/unit/umbel-wait-reasons.test.ts — a real test of the claim above, against real behaviour (no mocks of the unit under test). Do not implement anything yet. Run `bun test test/unit/umbel-wait-reasons.test.ts` yourself and confirm it FAILS for the right reason (the missing behaviour, not a typo or import error). The conductor will run `bun run check` and require it to be RED. Then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "impl",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c1)\n- (code) The umbel adapter's `wait` takes the reason from the JSON whatever the exit code: a wait that exits 124 with a timeout reason yields a `timeout` result, and the same for 122 provider-error, 123 idle, 125 dead and 126 input, each with its message; `WorkerSeamError` is thrown only when no reason parses, naming the exit code and the output · evidence: test/unit/umbel-wait-reasons.test.ts\n\n## How this node fits\nThe rule is the runner seam's: contracts/runner.md on jahala/plotplot, section 'A non-zero exit with a reason is a result' — cite it in the test's header. A RED TEST IS ALREADY DRAFTED and proven red against master for the right reason (five rows throw 'wait exited N' before the reason is read; three boundary cases pass): docs/dogfood/seam-keeps-tree/red/umbel-wait-reasons.test.ts.draft. In the RED phase copy it verbatim to the evidence path (cp), run it, confirm it fails as its header says, and change only what the code's real shapes force — say what in Tried. What it pins, for the implementation: a unit test with a scripted ExecFn: spawn + status succeed; send succeeds; `wait` returns {exitCode: N, output/stdout: '{\"reason\":\"<r>\",\"message\":\"<m>\"}\\n'} for each row (124 timeout, 122 provider-error, 123 idle, 125 dead, 126 input) → worker.wait() resolves with reason r and, where the adapter carries one, message m (extend gatherWorkerResult so provider-error, timeout and dead carry `message` too — the contract says every non-progress reason carries what was observed); exit 1 with 'umbel: tmux exploded' → rejects WorkerSeamError whose message holds 'exited 1' and the output; exit 0 with garbage → the existing unparseable error; the aborted-signal early answer is unchanged (test/integration/umbel-abort.test.ts stays green). Implementation: parse first; when stdout parses to an object with a string reason, that is the result whatever the exit code; otherwise throw as today, naming the exit code. Add 'provider-error' | 'file' | 'pattern' to WorkerResult's union in src/loop/deps.ts ONLY as far as typecheck needs here (the classifier is the third check's). MASTER'S CHECK IS RED ON THIS MACHINE before you touch anything (jahala/pleach#121): the installed umbel is rebuilt from its master, so test/integration/umbel-idle.test.ts (two tests) and the wedged-worker runPlan test over the umbel runner fail on this very defect and go green with your fix; and test/integration/umbel-abort.test.ts's sessionAlive helper reads `umbel status <name>`'s exit code as liveness, but since jahala/umbel#73 a killed session keeps a tombstone and status exits 0 with STATUS dead (exit 1 only after kill --purge) — make the helper read liveness (`umbel status <name> --json` or the STATUS column) without weakening what the test asserts. All four must be green when you finish; run them with the real binary, they are not skipped here. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: IMPLEMENT\nThe failing test at test/unit/umbel-wait-reasons.test.ts is in place. Now make it pass with the smallest honest change, per the How above and ENGINEERING.md. Do not weaken the test. Run `bun test test/unit/umbel-wait-reasons.test.ts` yourself, then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "green",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c1)\n- (code) The umbel adapter's `wait` takes the reason from the JSON whatever the exit code: a wait that exits 124 with a timeout reason yields a `timeout` result, and the same for 122 provider-error, 123 idle, 125 dead and 126 input, each with its message; `WorkerSeamError` is thrown only when no reason parses, naming the exit code and the output · evidence: test/unit/umbel-wait-reasons.test.ts\n\n## How this node fits\nThe rule is the runner seam's: contracts/runner.md on jahala/plotplot, section 'A non-zero exit with a reason is a result' — cite it in the test's header. A RED TEST IS ALREADY DRAFTED and proven red against master for the right reason (five rows throw 'wait exited N' before the reason is read; three boundary cases pass): docs/dogfood/seam-keeps-tree/red/umbel-wait-reasons.test.ts.draft. In the RED phase copy it verbatim to the evidence path (cp), run it, confirm it fails as its header says, and change only what the code's real shapes force — say what in Tried. What it pins, for the implementation: a unit test with a scripted ExecFn: spawn + status succeed; send succeeds; `wait` returns {exitCode: N, output/stdout: '{\"reason\":\"<r>\",\"message\":\"<m>\"}\\n'} for each row (124 timeout, 122 provider-error, 123 idle, 125 dead, 126 input) → worker.wait() resolves with reason r and, where the adapter carries one, message m (extend gatherWorkerResult so provider-error, timeout and dead carry `message` too — the contract says every non-progress reason carries what was observed); exit 1 with 'umbel: tmux exploded' → rejects WorkerSeamError whose message holds 'exited 1' and the output; exit 0 with garbage → the existing unparseable error; the aborted-signal early answer is unchanged (test/integration/umbel-abort.test.ts stays green). Implementation: parse first; when stdout parses to an object with a string reason, that is the result whatever the exit code; otherwise throw as today, naming the exit code. Add 'provider-error' | 'file' | 'pattern' to WorkerResult's union in src/loop/deps.ts ONLY as far as typecheck needs here (the classifier is the third check's). MASTER'S CHECK IS RED ON THIS MACHINE before you touch anything (jahala/pleach#121): the installed umbel is rebuilt from its master, so test/integration/umbel-idle.test.ts (two tests) and the wedged-worker runPlan test over the umbel runner fail on this very defect and go green with your fix; and test/integration/umbel-abort.test.ts's sessionAlive helper reads `umbel status <name>`'s exit code as liveness, but since jahala/umbel#73 a killed session keeps a tombstone and status exits 0 with STATUS dead (exit 1 only after kill --purge) — make the helper read liveness (`umbel status <name> --json` or the STATUS column) without weakening what the test asserts. All four must be green when you finish; run them with the real binary, they are not skipped here. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: GREEN\nRun `bun run check` (typecheck + biome + every test) and fix anything red — including biome formatting of your new files (`bunx biome check --write <file>` on files you created is fine). The conductor requires `bun run check` to exit 0 and will then run `/Users/jahala/.local/bin/weeder check --strict` on your diff (no deleted tests, no weakened assertions, no stubs/TODOs, no secrets, no files outside the work) and the tend2 verifier on your check. Then stop, ending your final message with the dated Tried line.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          }
        ]
      },
      "needs": [],
      "setup": "bun install --frozen-lockfile",
      "accept": {
        "smoke": "/Users/jahala/.local/bin/weeder check --strict",
        "audit": {
          "command": "node /tmp/tend2-pinned/dist/cli.js verify docs/tend2/seam-keeps-tree.tend2.html --repo-root . --check 1 --force --runner 'bun test {evidence}' --audit-egress --expect-payload 7806ba38b9ac",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "maxAttempts": 2,
        "timeoutMs": 1800000
      },
      "closes": [
        "seam-keeps-tree:c1"
      ]
    },
    {
      "id": "sk.settle",
      "worker": {
        "provider": "claude",
        "model": "claude-opus-5"
      },
      "work": {
        "test": "bun run check",
        "phases": [
          {
            "phase": "red",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c2)\n- (code) A surprise in a node's run settles the node instead of dropping it: a gate that throws after a green worker leaves `quarantine/<id>`, a receipt whose failed gate names the seam with the error's text as its tail, the attempt counted, and a verdict detail stating the next step; the tree is disposed only after it is kept, and `failed after 0 attempt(s)` with nothing kept cannot happen · evidence: test/loop/surprise-settles.test.ts\n\n## How this node fits\nThe rule is the runner seam's: contracts/runner.md on jahala/plotplot, section 'A non-zero exit with a reason is a result' — cite it in the test's header. A RED TEST IS ALREADY DRAFTED and proven red against master for the right reason (no verdict emitted, nothing quarantined, no receipt): docs/dogfood/seam-keeps-tree/red/surprise-settles.test.ts.draft — the harness needs no new knob, a waitScript that throws is a wait that throws. In the RED phase copy it verbatim to the evidence path (cp), run it, confirm it fails as its header says, and change only what the code's real shapes force — say what in Tried. It pins the names: gate.ran 'seam:audit' / 'seam:worker', exitCode -1, attempts 1, the seam gate as the receipt's last gate record with outputTailSha, the smoke's green record kept, detail carrying the text and 'pleach audit' (audit lane) or 'quarantine/<id>' (worker lane). Add case (c) below to it yourself. In run-node: a catch on the outer try, before the finally — for any error the attempt loop did not name (not PlanInvalidError from the preflight, which has no tree), hand back {verdict: {status: 'failed', attempts, gate: {ran: 'seam:<where>', exitCode: -1}, …}, gateOutputTail: the error's message (capped 2000), verdictDetail: '<message> — next: <step>'} through handBack so the live tree travels; <where> is the lane that threw (worker, smoke, audit — track a small 'lane' variable as the ladder advances); <step> is `pleach audit <plan> <node>` when the audit lane threw over a tree whose worker and smoke were green, else 're-run the plan: the node resumes from quarantine/<id>' (D17). attempts must count the attempt in flight (never 0 once a tree exists). In run-plan: the catch around runNode no longer returns — it builds the same failed outcome without a tree (iso absent) and calls settle, so a receipt is still written and the verdict line is journaled once, by settle. Read how settle treats an outcome with no iso (never-isolated failures) and keep the verdict line's provider/model/spawned fields. Prove on the harness: (a) a node whose audit worker's wait throws WorkerSeamError after a green worker and smoke → summary.failed=[id], summary.quarantined has it, the event log shows snapshot BEFORE dispose, the receipt exists with facts.status failed, its gates end with the seam gate carrying an outputTailSha, attempts ≥ 1, the verdict journal line's detail holds the error text and 'pleach audit'; (b) a worker wait that throws → same, next step names the re-run; (c) a throw before isolation (spawnWorker for a node never isolated is not reachable — use an isolate that throws a non-catastrophic error) → failed verdict, receipt written, no quarantine, no unhandled rejection. Add harness knobs like the existing ones (per-node waitThrows / auditWaitThrows carrying a message). Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: RED\nWrite the failing test FIRST at test/loop/surprise-settles.test.ts — a real test of the claim above, against real behaviour (no mocks of the unit under test). Do not implement anything yet. Run `bun test test/loop/surprise-settles.test.ts` yourself and confirm it FAILS for the right reason (the missing behaviour, not a typo or import error). The conductor will run `bun run check` and require it to be RED. Then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "impl",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c2)\n- (code) A surprise in a node's run settles the node instead of dropping it: a gate that throws after a green worker leaves `quarantine/<id>`, a receipt whose failed gate names the seam with the error's text as its tail, the attempt counted, and a verdict detail stating the next step; the tree is disposed only after it is kept, and `failed after 0 attempt(s)` with nothing kept cannot happen · evidence: test/loop/surprise-settles.test.ts\n\n## How this node fits\nThe rule is the runner seam's: contracts/runner.md on jahala/plotplot, section 'A non-zero exit with a reason is a result' — cite it in the test's header. A RED TEST IS ALREADY DRAFTED and proven red against master for the right reason (no verdict emitted, nothing quarantined, no receipt): docs/dogfood/seam-keeps-tree/red/surprise-settles.test.ts.draft — the harness needs no new knob, a waitScript that throws is a wait that throws. In the RED phase copy it verbatim to the evidence path (cp), run it, confirm it fails as its header says, and change only what the code's real shapes force — say what in Tried. It pins the names: gate.ran 'seam:audit' / 'seam:worker', exitCode -1, attempts 1, the seam gate as the receipt's last gate record with outputTailSha, the smoke's green record kept, detail carrying the text and 'pleach audit' (audit lane) or 'quarantine/<id>' (worker lane). Add case (c) below to it yourself. In run-node: a catch on the outer try, before the finally — for any error the attempt loop did not name (not PlanInvalidError from the preflight, which has no tree), hand back {verdict: {status: 'failed', attempts, gate: {ran: 'seam:<where>', exitCode: -1}, …}, gateOutputTail: the error's message (capped 2000), verdictDetail: '<message> — next: <step>'} through handBack so the live tree travels; <where> is the lane that threw (worker, smoke, audit — track a small 'lane' variable as the ladder advances); <step> is `pleach audit <plan> <node>` when the audit lane threw over a tree whose worker and smoke were green, else 're-run the plan: the node resumes from quarantine/<id>' (D17). attempts must count the attempt in flight (never 0 once a tree exists). In run-plan: the catch around runNode no longer returns — it builds the same failed outcome without a tree (iso absent) and calls settle, so a receipt is still written and the verdict line is journaled once, by settle. Read how settle treats an outcome with no iso (never-isolated failures) and keep the verdict line's provider/model/spawned fields. Prove on the harness: (a) a node whose audit worker's wait throws WorkerSeamError after a green worker and smoke → summary.failed=[id], summary.quarantined has it, the event log shows snapshot BEFORE dispose, the receipt exists with facts.status failed, its gates end with the seam gate carrying an outputTailSha, attempts ≥ 1, the verdict journal line's detail holds the error text and 'pleach audit'; (b) a worker wait that throws → same, next step names the re-run; (c) a throw before isolation (spawnWorker for a node never isolated is not reachable — use an isolate that throws a non-catastrophic error) → failed verdict, receipt written, no quarantine, no unhandled rejection. Add harness knobs like the existing ones (per-node waitThrows / auditWaitThrows carrying a message). Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: IMPLEMENT\nThe failing test at test/loop/surprise-settles.test.ts is in place. Now make it pass with the smallest honest change, per the How above and ENGINEERING.md. Do not weaken the test. Run `bun test test/loop/surprise-settles.test.ts` yourself, then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "green",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c2)\n- (code) A surprise in a node's run settles the node instead of dropping it: a gate that throws after a green worker leaves `quarantine/<id>`, a receipt whose failed gate names the seam with the error's text as its tail, the attempt counted, and a verdict detail stating the next step; the tree is disposed only after it is kept, and `failed after 0 attempt(s)` with nothing kept cannot happen · evidence: test/loop/surprise-settles.test.ts\n\n## How this node fits\nThe rule is the runner seam's: contracts/runner.md on jahala/plotplot, section 'A non-zero exit with a reason is a result' — cite it in the test's header. A RED TEST IS ALREADY DRAFTED and proven red against master for the right reason (no verdict emitted, nothing quarantined, no receipt): docs/dogfood/seam-keeps-tree/red/surprise-settles.test.ts.draft — the harness needs no new knob, a waitScript that throws is a wait that throws. In the RED phase copy it verbatim to the evidence path (cp), run it, confirm it fails as its header says, and change only what the code's real shapes force — say what in Tried. It pins the names: gate.ran 'seam:audit' / 'seam:worker', exitCode -1, attempts 1, the seam gate as the receipt's last gate record with outputTailSha, the smoke's green record kept, detail carrying the text and 'pleach audit' (audit lane) or 'quarantine/<id>' (worker lane). Add case (c) below to it yourself. In run-node: a catch on the outer try, before the finally — for any error the attempt loop did not name (not PlanInvalidError from the preflight, which has no tree), hand back {verdict: {status: 'failed', attempts, gate: {ran: 'seam:<where>', exitCode: -1}, …}, gateOutputTail: the error's message (capped 2000), verdictDetail: '<message> — next: <step>'} through handBack so the live tree travels; <where> is the lane that threw (worker, smoke, audit — track a small 'lane' variable as the ladder advances); <step> is `pleach audit <plan> <node>` when the audit lane threw over a tree whose worker and smoke were green, else 're-run the plan: the node resumes from quarantine/<id>' (D17). attempts must count the attempt in flight (never 0 once a tree exists). In run-plan: the catch around runNode no longer returns — it builds the same failed outcome without a tree (iso absent) and calls settle, so a receipt is still written and the verdict line is journaled once, by settle. Read how settle treats an outcome with no iso (never-isolated failures) and keep the verdict line's provider/model/spawned fields. Prove on the harness: (a) a node whose audit worker's wait throws WorkerSeamError after a green worker and smoke → summary.failed=[id], summary.quarantined has it, the event log shows snapshot BEFORE dispose, the receipt exists with facts.status failed, its gates end with the seam gate carrying an outputTailSha, attempts ≥ 1, the verdict journal line's detail holds the error text and 'pleach audit'; (b) a worker wait that throws → same, next step names the re-run; (c) a throw before isolation (spawnWorker for a node never isolated is not reachable — use an isolate that throws a non-catastrophic error) → failed verdict, receipt written, no quarantine, no unhandled rejection. Add harness knobs like the existing ones (per-node waitThrows / auditWaitThrows carrying a message). Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: GREEN\nRun `bun run check` (typecheck + biome + every test) and fix anything red — including biome formatting of your new files (`bunx biome check --write <file>` on files you created is fine). The conductor requires `bun run check` to exit 0 and will then run `/Users/jahala/.local/bin/weeder check --strict` on your diff (no deleted tests, no weakened assertions, no stubs/TODOs, no secrets, no files outside the work) and the tend2 verifier on your check. Then stop, ending your final message with the dated Tried line.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          }
        ]
      },
      "needs": [
        "sk.reason"
      ],
      "setup": "bun install --frozen-lockfile",
      "accept": {
        "smoke": "/Users/jahala/.local/bin/weeder check --strict",
        "audit": {
          "command": "node /tmp/tend2-pinned/dist/cli.js verify docs/tend2/seam-keeps-tree.tend2.html --repo-root . --check 2 --force --runner 'bun test {evidence}' --audit-egress --expect-payload 7806ba38b9ac",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "maxAttempts": 2,
        "timeoutMs": 1800000
      },
      "closes": [
        "seam-keeps-tree:c2"
      ]
    },
    {
      "id": "sk.contract",
      "worker": {
        "provider": "claude",
        "model": "claude-opus-5"
      },
      "work": {
        "test": "bun run check",
        "phases": [
          {
            "phase": "red",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c3)\n- (code) `WorkerResult.reason` and `classifyWorkerReason` name every reason of `contracts/runner.md` and classify each as its fixture says — provider-error retryable, file and pattern terminal — pinned by a vendored copy of the fixture; a reason the table lacks falls to terminal and is journaled as `seam-violation` with the reason's text · evidence: test/unit/runner-contract.test.ts\n\n## How this node fits\nVendor the fixture as test/fixtures/runner-reasons.jsonl — the nine lines quoted in the ground truth's source (reason, exitCode, meaning, classification: stop 0 terminal; file 0 terminal; pattern 0 terminal; provider-error 122 retryable; idle 123 blocked; timeout 124 retryable; dead 125 dead; input 126 blocked; aborted 130 terminal), with a header comment line is NOT allowed in JSONL, so put the provenance (contracts/fixtures/runner/reasons.jsonl on jahala/plotplot, the umbrella's seam test contracts/test/runner.test.sh compares the live copies) in the test file's doc comment. The test: for every fixture row, classify({kind: 'worker', reason}) equals the row's classification; the union in src/loop/deps.ts names every fixture reason (read the file and apply the SAME regex the umbrella's test uses: the first /reason\\?:\\s*([^;]+);/ match, quoted literals) and classifyWorkerReason's source lists a `case` for each (same regex shapes as the ground truth says) — so a refactor that would break the seam test breaks this one first. Then the unknown reason: WorkerReason stays a closed union for callers, but the adapter can hand over any string; where run-node classifies a non-stop result, a reason outside the union is journaled {event: 'seam-violation', node, reason, detail: 'umbel wait returned a reason contracts/runner.md does not name'} (KINDS: node.lifecycle; the docs row is the next check's) and classified terminal. Put the known-reason list in one place (an exported const array in src/core/classify.ts from which the type derives is fine, as long as the deps.ts inline union and the switch keep their regex-readable shapes). provider-error then rides the existing retryable path (same tree, evidence = the message) — assert that on the harness in this same file or beside it only if it is red; do not write a test that passes on arrival. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: RED\nWrite the failing test FIRST at test/unit/runner-contract.test.ts — a real test of the claim above, against real behaviour (no mocks of the unit under test). Do not implement anything yet. Run `bun test test/unit/runner-contract.test.ts` yourself and confirm it FAILS for the right reason (the missing behaviour, not a typo or import error). The conductor will run `bun run check` and require it to be RED. Then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "impl",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c3)\n- (code) `WorkerResult.reason` and `classifyWorkerReason` name every reason of `contracts/runner.md` and classify each as its fixture says — provider-error retryable, file and pattern terminal — pinned by a vendored copy of the fixture; a reason the table lacks falls to terminal and is journaled as `seam-violation` with the reason's text · evidence: test/unit/runner-contract.test.ts\n\n## How this node fits\nVendor the fixture as test/fixtures/runner-reasons.jsonl — the nine lines quoted in the ground truth's source (reason, exitCode, meaning, classification: stop 0 terminal; file 0 terminal; pattern 0 terminal; provider-error 122 retryable; idle 123 blocked; timeout 124 retryable; dead 125 dead; input 126 blocked; aborted 130 terminal), with a header comment line is NOT allowed in JSONL, so put the provenance (contracts/fixtures/runner/reasons.jsonl on jahala/plotplot, the umbrella's seam test contracts/test/runner.test.sh compares the live copies) in the test file's doc comment. The test: for every fixture row, classify({kind: 'worker', reason}) equals the row's classification; the union in src/loop/deps.ts names every fixture reason (read the file and apply the SAME regex the umbrella's test uses: the first /reason\\?:\\s*([^;]+);/ match, quoted literals) and classifyWorkerReason's source lists a `case` for each (same regex shapes as the ground truth says) — so a refactor that would break the seam test breaks this one first. Then the unknown reason: WorkerReason stays a closed union for callers, but the adapter can hand over any string; where run-node classifies a non-stop result, a reason outside the union is journaled {event: 'seam-violation', node, reason, detail: 'umbel wait returned a reason contracts/runner.md does not name'} (KINDS: node.lifecycle; the docs row is the next check's) and classified terminal. Put the known-reason list in one place (an exported const array in src/core/classify.ts from which the type derives is fine, as long as the deps.ts inline union and the switch keep their regex-readable shapes). provider-error then rides the existing retryable path (same tree, evidence = the message) — assert that on the harness in this same file or beside it only if it is red; do not write a test that passes on arrival. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: IMPLEMENT\nThe failing test at test/unit/runner-contract.test.ts is in place. Now make it pass with the smallest honest change, per the How above and ENGINEERING.md. Do not weaken the test. Run `bun test test/unit/runner-contract.test.ts` yourself, then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "green",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c3)\n- (code) `WorkerResult.reason` and `classifyWorkerReason` name every reason of `contracts/runner.md` and classify each as its fixture says — provider-error retryable, file and pattern terminal — pinned by a vendored copy of the fixture; a reason the table lacks falls to terminal and is journaled as `seam-violation` with the reason's text · evidence: test/unit/runner-contract.test.ts\n\n## How this node fits\nVendor the fixture as test/fixtures/runner-reasons.jsonl — the nine lines quoted in the ground truth's source (reason, exitCode, meaning, classification: stop 0 terminal; file 0 terminal; pattern 0 terminal; provider-error 122 retryable; idle 123 blocked; timeout 124 retryable; dead 125 dead; input 126 blocked; aborted 130 terminal), with a header comment line is NOT allowed in JSONL, so put the provenance (contracts/fixtures/runner/reasons.jsonl on jahala/plotplot, the umbrella's seam test contracts/test/runner.test.sh compares the live copies) in the test file's doc comment. The test: for every fixture row, classify({kind: 'worker', reason}) equals the row's classification; the union in src/loop/deps.ts names every fixture reason (read the file and apply the SAME regex the umbrella's test uses: the first /reason\\?:\\s*([^;]+);/ match, quoted literals) and classifyWorkerReason's source lists a `case` for each (same regex shapes as the ground truth says) — so a refactor that would break the seam test breaks this one first. Then the unknown reason: WorkerReason stays a closed union for callers, but the adapter can hand over any string; where run-node classifies a non-stop result, a reason outside the union is journaled {event: 'seam-violation', node, reason, detail: 'umbel wait returned a reason contracts/runner.md does not name'} (KINDS: node.lifecycle; the docs row is the next check's) and classified terminal. Put the known-reason list in one place (an exported const array in src/core/classify.ts from which the type derives is fine, as long as the deps.ts inline union and the switch keep their regex-readable shapes). provider-error then rides the existing retryable path (same tree, evidence = the message) — assert that on the harness in this same file or beside it only if it is red; do not write a test that passes on arrival. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: GREEN\nRun `bun run check` (typecheck + biome + every test) and fix anything red — including biome formatting of your new files (`bunx biome check --write <file>` on files you created is fine). The conductor requires `bun run check` to exit 0 and will then run `/Users/jahala/.local/bin/weeder check --strict` on your diff (no deleted tests, no weakened assertions, no stubs/TODOs, no secrets, no files outside the work) and the tend2 verifier on your check. Then stop, ending your final message with the dated Tried line.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          }
        ]
      },
      "needs": [
        "sk.settle"
      ],
      "setup": "bun install --frozen-lockfile",
      "accept": {
        "smoke": "/Users/jahala/.local/bin/weeder check --strict",
        "audit": {
          "command": "node /tmp/tend2-pinned/dist/cli.js verify docs/tend2/seam-keeps-tree.tend2.html --repo-root . --check 3 --force --runner 'bun test {evidence}' --audit-egress --expect-payload 7806ba38b9ac",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "maxAttempts": 2,
        "timeoutMs": 1800000
      },
      "closes": [
        "seam-keeps-tree:c3"
      ]
    },
    {
      "id": "sk.docs",
      "worker": {
        "provider": "claude",
        "model": "claude-opus-5"
      },
      "work": {
        "test": "bun run check",
        "phases": [
          {
            "phase": "red",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c4)\n- (code) docs/journal.md documents `seam-violation` and the surprise verdict's gate and detail; docs/adapters.md states the runner contract — the reason is read from the JSON whatever the exit code, the nine reasons and their classes, citing `contracts/runner.md`; `KINDS` pins the event · evidence: test/unit/journal-doc.test.ts\n\n## How this node fits\nThe pin test exists; extend it first with the assertions that make it red (a seam-violation row; the verdict row mentions the seam gate `seam:<lane>` and the next step in detail; docs/adapters.md names all nine reasons with their classes and the sentence that the reason is read whatever the exit code, and cites contracts/runner.md), then write the docs: docs/journal.md rows, docs/adapters.md's umbel section (a small table: reason, exit, class), README's one paragraph on what a failed node keeps if it has one to touch; keep test/e2e/help.test.ts green. One sentence per behaviour. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: RED\nWrite the failing test FIRST at test/unit/journal-doc.test.ts — a real test of the claim above, against real behaviour (no mocks of the unit under test). Do not implement anything yet. Run `bun test test/unit/journal-doc.test.ts` yourself and confirm it FAILS for the right reason (the missing behaviour, not a typo or import error). The conductor will run `bun run check` and require it to be RED. Then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "impl",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c4)\n- (code) docs/journal.md documents `seam-violation` and the surprise verdict's gate and detail; docs/adapters.md states the runner contract — the reason is read from the JSON whatever the exit code, the nine reasons and their classes, citing `contracts/runner.md`; `KINDS` pins the event · evidence: test/unit/journal-doc.test.ts\n\n## How this node fits\nThe pin test exists; extend it first with the assertions that make it red (a seam-violation row; the verdict row mentions the seam gate `seam:<lane>` and the next step in detail; docs/adapters.md names all nine reasons with their classes and the sentence that the reason is read whatever the exit code, and cites contracts/runner.md), then write the docs: docs/journal.md rows, docs/adapters.md's umbel section (a small table: reason, exit, class), README's one paragraph on what a failed node keeps if it has one to touch; keep test/e2e/help.test.ts green. One sentence per behaviour. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: IMPLEMENT\nThe failing test at test/unit/journal-doc.test.ts is in place. Now make it pass with the smallest honest change, per the How above and ENGINEERING.md. Do not weaken the test. Run `bun test test/unit/journal-doc.test.ts` yourself, then stop.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          },
          {
            "phase": "green",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c4)\n- (code) docs/journal.md documents `seam-violation` and the surprise verdict's gate and detail; docs/adapters.md states the runner contract — the reason is read from the JSON whatever the exit code, the nine reasons and their classes, citing `contracts/runner.md`; `KINDS` pins the event · evidence: test/unit/journal-doc.test.ts\n\n## How this node fits\nThe pin test exists; extend it first with the assertions that make it red (a seam-violation row; the verdict row mentions the seam gate `seam:<lane>` and the next step in detail; docs/adapters.md names all nine reasons with their classes and the sentence that the reason is read whatever the exit code, and cites contracts/runner.md), then write the docs: docs/journal.md rows, docs/adapters.md's umbel section (a small table: reason, exit, class), README's one paragraph on what a failed node keeps if it has one to touch; keep test/e2e/help.test.ts green. One sentence per behaviour. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: GREEN\nRun `bun run check` (typecheck + biome + every test) and fix anything red — including biome formatting of your new files (`bunx biome check --write <file>` on files you created is fine). The conductor requires `bun run check` to exit 0 and will then run `/Users/jahala/.local/bin/weeder check --strict` on your diff (no deleted tests, no weakened assertions, no stubs/TODOs, no secrets, no files outside the work) and the tend2 verifier on your check. Then stop, ending your final message with the dated Tried line.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          }
        ]
      },
      "needs": [
        "sk.contract"
      ],
      "setup": "bun install --frozen-lockfile",
      "accept": {
        "smoke": "/Users/jahala/.local/bin/weeder check --strict",
        "audit": {
          "command": "node /tmp/tend2-pinned/dist/cli.js verify docs/tend2/seam-keeps-tree.tend2.html --repo-root . --check 4 --force --runner 'bun test {evidence}' --audit-egress --expect-payload 7806ba38b9ac",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "maxAttempts": 2,
        "timeoutMs": 1800000
      },
      "closes": [
        "seam-keeps-tree:c4"
      ]
    },
    {
      "id": "sk.e2e",
      "worker": {
        "provider": "claude",
        "model": "claude-opus-5"
      },
      "work": {
        "test": "bun run check",
        "phases": [
          {
            "phase": "green",
            "prompt": "You are a worker in a build loop. Make the check below pass, honestly. Nobody reviews your prose — only the verifier's verdict counts.\n\n## The loop\nA seam's surprise never costs the tree (ledger D23; loop docs/tend2/seam-keeps-tree.tend2.html). Whatever a runner says or fails to say, the tree a node built is kept: the umbel adapter reads the wait's reason from the JSON whatever the exit code; every reason of the runner contract is named and classified as the contract says, provider-error retryable; a surprise anywhere in a node's run settles the node — attempt counted, tree quarantined, a receipt with the seam's text as the reason and the next step — so `pleach audit` or a re-run starts from what was built.\n\n## Your check (c5)\n- (code) Through the real CLI with a fake umbel binary: a worker wait that exits 124 with a timeout reason is retried and then settles failed with its tree quarantined, a receipt and both attempts counted; a wait that exits 122 provider-error is retried; an auditor whose wait times out over a green tree leaves the tree quarantined with a never-adjudicated audit record and `pleach audit` re-runs the lane alone; a wait that exits 1 with no reason leaves a quarantine and a receipt carrying the seam's text and the next step · evidence: test/e2e/seam-keeps-tree.test.ts\n\n## How this node fits\nModel on test/e2e/quarantine.test.ts and record-survives.test.ts (real git in a tmp repo, the CLI spawned as `bun src/main.ts`), with --umbel-bin pointing at a fake umbel the test writes into its tmp dir: a bash script answering the verbs the adapter calls (spawn → 'spawned: <name>', status → exit 0, send --json → {\"sinceMtime\":0}, wait --json → scripted by a per-case file: the JSON line to print and the exit code, optionally different per call so a first wait can time out and a second stop; read → a final message ending with a dated Tried line; actions/diff → empty; kill → exit 0), and whose 'worker' edits a file in the cwd on send so the tree holds work. Cases: (a) wait exits 124 {\"reason\":\"timeout\"} twice under maxAttempts 2 → exit 1, summary.failed, quarantine/<id> holds the file, receipt facts.status failed with attempts 2; (b) first wait 122 provider-error, second stop → node closes, journal shows the retry; (c) worker stops green, smoke passes, the AUDITOR's wait exits 124 → the node does not close, quarantine/<id> holds the tree, the receipt's audit record says never adjudicated with the auditor's reason, then `pleach audit <plan> <node>` with a fake whose audit wait now stops with a passing tend-audit-result closes it; (d) wait exits 1 printing 'umbel: tmux exploded' → exit 1, quarantine/<id> exists, the receipt's last gate is the seam gate, the verdict journal line's detail carries the text and the next step. This node is the sink of the loop: its audit verifies EVERY check on the page. Ground truth, read before coding (master 411a1eb): umbel's `wait --json` prints {\"reason\": …, \"message\"?: …} on stdout and exits 0 for stop/file/pattern, 122 provider-error, 123 idle, 124 timeout, 125 dead, 126 input, 130 aborted (contracts/runner.md on jahala/plotplot; a copy of its fixture contracts/fixtures/runner/reasons.jsonl is nine JSON lines {reason, exitCode, meaning, classification}). src/adapters/umbel.ts wait(): after exec, an aborted signal answers `aborted` first (D16, keep); then `if (result.exitCode !== 0) throw new WorkerSeamError('wait exited N: …')` BEFORE the JSON is parsed — the defect; then parse, require a string `reason`, gatherWorkerResult(name, cwd, reason, message) (only `stop` does the read/actions/diff roundtrips; input/idle carry `message`). src/loop/deps.ts WorkerResult has `reason?: 'stop' | 'dead' | 'timeout' | 'aborted' | 'input' | 'idle';` and src/core/classify.ts has `export type WorkerReason` plus `function classifyWorkerReason(reason)` as a switch of `case 'x':` lines followed by `return 'klass';` with a default → terminal. The umbrella's seam test (contracts/test/runner.test.sh) reads BOTH by regex and must keep working: the FIRST `reason?: … ;` in src/loop/deps.ts must be the inline union of quoted literals, and classifyWorkerReason must stay a function whose cases are `case '<reason>':` lines and whose answers are `return '<class>';` lines — keep those shapes. src/loop/run-node.ts runNode(): one outer `try { for (;;) { …attempt… } } finally { if (iso !== null) await disposeQuiet(iso); }` — every named end goes through handBack(result), which moves the live `iso` into the result (with gates, audit records, staged set, handback, redSealedAt) so run-plan's settle() can quarantine-then-dispose; any OTHER throw (the `throw err;` after the GateCannotRunError/GateFailedError branches around runWork, a throw from the smoke or audit ladder, a WorkerSeamError from the audit worker's wait inside runAudit) escapes, and the finally disposes the tree. src/loop/run-plan.ts: the catch around runNode journals a `verdict` failed with `detail` and returns before `settle(node, outcome, startedAt, resumed, spawned)` — settle is the only writer of the receipt, the quarantine (quarantineOrJournal → snapshot, D21) and the kept artifacts. The refused-commit path in settle (D21, test/loop/commit-refused.test.ts) is the precedent for 'keep, then dispose' with `gate.ran` naming what failed and the text as the tail. After a non-stop worker result run-node classifies: dead → dead (fallback provider, D17), retryable → same-tree retry with evidence while attempts remain, blocked → hand back blocked; the audit lane turns a non-stop auditor into {kind: 'worker-fault'} → unadjudicated records (`auditor <reason> — never adjudicated`). The loop tests use the in-memory seams of test/loop/harness.ts (real implementations of the seam interfaces; it has per-node knobs for worker results, commitBranch throwing, dispose throwing — add knobs the same way, never behaviour mocks). Unit adapter tests script ExecFn (see test/unit/umbel-seam.test.ts makeRecordingExec). Journal events need a docs/journal.md row and a KINDS entry (src/core/journal-envelope.ts; node-level → node.lifecycle), pinned by test/unit/journal-doc.test.ts. The Plan schema is untouched by this loop.\n\n## Your environment\n- You are at the repository root of an isolated git worktree of pleach. Work here directly.\n- Read ENGINEERING.md FIRST and conform to it (it is binding: test-first, no stubs/mocks/TODOs, S.U.P.E.R. layering, typed errors, no bare `new Error`, never `rm`). Then read docs/ledger.md item D23 and the loop file docs/tend2/seam-keeps-tree.tend2.html including its narrative and Tried — it is your full context and it is READ-ONLY for you (check states belong to the verifier; a Tried line moves the payload pin).\n- Leave your changes in the working tree; do not create branches, commit, or push.\n- `bun run check` (typecheck + biome + every test) is the repository's green; it must be green when you finish.\n- Never run `bun link`, and never touch /tmp/pleach-pinned or /tmp/tend2-pinned: they are the conductor and the verifier driving you.\n- Probe/scratch files go in `.loop-scratch/` only. If genuinely blocked, write BLOCKED.md at the repo root — what you tried, what stopped you, what a fix needs — instead of faking a green.\n\n## This phase: PROOF (green only)\nThis node proves the whole loop end to end. Its behaviours were delivered by the sibling nodes, so the failing-test gate does not apply: the conductor requires only that `bun run check` exits 0 with your proof in place at test/e2e/seam-keeps-tree.test.ts. Write the proof so every case the claim names is exercised against real behaviour. If a case is red, a sibling left a gap: fix it here, smallest honest change, and say so in Tried. If your tree was resumed from a quarantined attempt and holds a BLOCKED.md the resume resolves, remove it (`git rm` if tracked, else `trash`). Run `bun run check` and fix anything red, including biome formatting of files you created. The conductor will then run `/Users/jahala/.local/bin/weeder check --strict` on your diff and the tend2 verifier on every check of the page. Then stop, ending your final message with the dated Tried line.\n\n## Rules\n- Solve the CLASS of problem the claim names, not the test's example inputs; no answer keys.\n- Never weaken, skip, or mock the unit under test; the loop tests use the in-memory seams in test/loop/harness.ts (real implementations of the seam interfaces), never behaviour mocks.\n- Production quality only; match the surrounding style; the smallest change that is honest.\n- Your FINAL message must end with one line of the form `Tried: YYYY-MM-DD <what you did, what you rejected and why, anything the next worker must know>` — the conductor transcribes it into the loop page."
          }
        ]
      },
      "needs": [
        "sk.docs"
      ],
      "setup": "bun install --frozen-lockfile",
      "accept": {
        "smoke": "/Users/jahala/.local/bin/weeder check --strict",
        "audit": {
          "command": "node /tmp/tend2-pinned/dist/cli.js verify docs/tend2/seam-keeps-tree.tend2.html --repo-root . --force --runner 'bun test {evidence}' --audit-egress --expect-payload 7806ba38b9ac",
          "provider": "codex",
          "selfIntegrity": true
        }
      },
      "policy": {
        "maxAttempts": 2,
        "timeoutMs": 2700000
      },
      "closes": [
        "seam-keeps-tree:c5"
      ]
    }
  ]
}
