# Dogfood — gate-artifacts (D14, jahala/pleach#59)

What the pleach agent hit in tend2, pleach, umbel, weeder and pollen while conducting this loop.
The shaping-pass findings that apply to every loop of the 2026-09-08 order are recorded once in
`test-phase-commit.md`; this file carries what this loop's run added.

## Conducting (2026-09-10)

Run: `pleach run docs/dogfood/gate-artifacts/plan.json --repo-root . --max-concurrency 1`, workers
claude-opus-5 through umbel, smoke `weeder check --strict`, audit = tend2 verify on the node's check
as a self-integral audit run by opencode + deepseek/deepseek-v4-pro. Two launches by design: the
collection fix (ga.collect) ran ALONE from a one-node sub-plan, landed, and the conductor restarted
on the landed code before the other six (a conductor's own code does not change mid-run; #74/#79 had
just cost the umbrella forty minutes of a build). Result: 7 of 7 nodes verified in 8 attempts; landed
by `pleach land` (fast-forward, land gate green) onto feat/gate-artifacts at 0b03a24; `bun run check`
green (509 tests); every check stamped by `tend2 verify`.

This was the first loop conducted under D13: every `node/<id>` reads base → red → verified, the
red commit holding the test alone with the `pleach-phase: red` trailer.

| node | check | attempts | wall | what the extra attempt was |
|---|---|---|---|---|
| ga.collect | c1 | 1 | 13m14s | — (ran alone, landed first) |
| ga.exec-stdout | c2 | 1 | 7m40s | — |
| ga.parse-sarif | c3 | 1 | 8m45s | — |
| ga.artifact-sha | c4 | 1 | 10m44s | — |
| ga.settle-write | c5 | 1 | 10m17s | — |
| ga.friction | c6 | 1 | 10m05s | — |
| ga.e2e | c7 | 2 | 14m26s | the RED gate refused a proof that already passed; the retry found the stale-artifact gap |

### Findings, per node
### ga.collect (run alone, first)
- FIRST REAL RED SEAL in history: e11dbee "pleach: ga.collect red phase" / body "test: bun run check" / trailer "pleach-phase: red"; tree = the test file only (321 lines). D13 works on a real conducted node.
- The seal's journaled `files` carries the same path twice: absolute (umbel's manifest) and relative (git). `dedup` compares strings. Harmless for git add inside the worktree, but it double-counts and is the same absolute-path family as #74 — which this very node normalises (absolute inside cwd → relative). Expect the next seal to list it once.
- ga.collect VERIFIED (1 attempt, 13m14s, audit pass); node/ga.collect = base → e11dbee (red) → a5c7b4f (verified). Landed by `pleach land` (ff, land gate green) onto feat/gate-artifacts; bun run check 440 tests green. The conductor now runs the landed code for the rest of the loop.
- Diff: src/core/delivery.ts (pure partitionDelivery: .loop-scratch/**, .plotplot/friction/**, outside-the-tree → set aside; absolute-inside-cwd → relative), IsolateSeam.ignored via `git check-ignore` WITH the index (a tracked file matching an ignore rule stays delivery — subtle and right), dedup after normalisation (closes the double-count), used by both the close and the seal, journal `set-aside` documented. Reviewed.
- cape-town halted all conductors mid-node (four ran at once and burned half the window): this node was the only one in my plan, so nothing further dispatched.
- ga.exec-stdout VERIFIED (1 attempt, 7m40s). Seal lists the test once, relative; receipt stagedFiles (10) now equals the diff's file count — the collector's normalisation closed #73's double-count half. Diff: ExecResult.stdout filled by the seam's own stdout drain; guard-hit and spawn-failure results carry stdout ''. Reviewed.
- ga.parse-sarif VERIFIED (1 attempt, 8m45s). Pure total parseSarif; a real weeder SARIF fixture vendored. Reviewed.
- ga.artifact-sha VERIFIED (1 attempt, 10m44s). artifactSha on the smoke GateRecord when stdout parses as SARIF (the parse decides, not the exit code — a red weeder run's log is kept too); the bytes ride RunNodeResult.smokeStdout to settle; every existing receipt hashes unchanged (canonicalJson drops undefined). Reviewed.
- ga.settle-write VERIFIED (1 attempt, 10m17s): keepArtifactsOrJournal before dispose on BOTH the done and quarantine paths (a red strict gate's log is exactly what the calibration folds want); receipt.artifacts outside the envelope; write failure → receipt-write-failed, close stands; gate-artifact documented. Reviewed.
- ga.friction VERIFIED (1 attempt, 10m05s): IsolateSeam.readFriction (month files only, filename order); one keepOrJournal for both artifacts so a failure of one never loses the other. Reviewed.

- ga.e2e VERIFIED (2 attempts, 14m26s, whole-page audit 7/7 pass). Attempt 1's first proof already passed against the landed siblings; the RED gate refused it. The retry's honest red: a node re-closed under acceptance evolution kept the EARLIER close's `.sarif`/`.friction.jsonl` under its name — a stale artifact read as this close's. Added `ReceiptStore.discardArtifact`, called when a close keeps nothing. Second time this loop-family's RED gate turned a "test passes already" into a real finding (loop 1: the porcelain collapse).

### Tools, this run
- pleach: the one-node sub-plan workaround for "land node 1, restart the conductor" → #85. Resume narration → #86. The conductor's build is invisible in the record → #87.
- pleach: `set-aside` never fired during this loop — no worker wrote scratch through the editor — so the fix is proven by its tests and the e2e, not by a live catch. Its first live catch will be on the umbrella's runs once this lands on master.
- tend2, umbel, weeder, pollen: nothing new beyond the issue round filed 2026-09-10 (tend#188, umbel#67 comment, weeder#33, pollen#23). `weeder check --strict` was green on all seven nodes; the SARIF it printed is what ga.e2e keeps.

### Issues touched from this loop
- pleach #59 (the loop; closed with the merge commit), #74/#76/#79 (the class ga.collect closes — closed with the merge commit), #73 (comment: half closed), #85, #86, #87 (new).
- weeder #33, pollen #23, tend #188 (new); umbel #67, tend #183 (comments).

## Tried lines from the workers' handbacks (every phase, verbatim)

One line per node — the implementation phase's — is transcribed onto the page, marked as
transcribed. Every phase's line is kept here. Recovered from Claude Code's own transcripts
(jahala/pleach#66).

#### ga.collect (c1)
- (RED) Tried: 2026-09-09 wrote the RED test at test/integration/collect-set-aside.test.ts (pure partitionDelivery block + real-git ignored() block + a full runPlan close against real git with real seams and a file-writing runner); confirmed both catastrophes reproduce today (ls-files exit 128 on the absolute outside path, git add exit 1 on the ignored scratch/run.log) and that only the missing module and missing seam method are typecheck-red. Rejected `git check-ignore --no-index` from the node brief — probed real git: it reports tracked files matching an ignore pattern as ignored, which would silently drop delivery; the test pins index-aware `git check-ignore` instead. Rejected mocking the isolate seam here (integration = real substrate) and rejected putting the pure block in test/unit (the check's single evidence path must cover it).
- (IMPL) Tried: 2026-09-09 implemented D14 collection — pure `partitionDelivery` in src/core/delivery.ts, `ignored()` on the isolate seam, one `collectDelivery` helper in run-node applied to BOTH the close's staging and the red-phase seal, `set-aside` journaled and documented, harness fixture `ignoredByNode` exercised by one loop test; rejected `--no-index` on `git check-ignore` (probed real git: it reports tracked files matching an ignore rule as ignored, which would drop delivery — index-aware is correct and the test pins it), rejected `-z` output parsing (git refuses `-z` without `--stdin`, and ExecFn has no stdin) in favour of `-c core.quotePath=false` plus intersecting the result with the caller's paths so quoting or stderr noise can never invent a set-aside path; next worker: `collectDelivery` is the single collection point — the SARIF/friction artifact work must not re-introduce a second staging path, and note the friction journal is set aside from delivery but still reaches quarantine via `changedFiles`.
- (GREEN) Tried: 2026-09-09 GREEN phase — ran `bun run check` (typecheck + biome + 440 tests) and `weeder check --strict`, both clean on the first pass, so nothing needed fixing; kept the implementation exactly as landed in the previous phase (pure `partitionDelivery` in core, index-aware `git check-ignore` in the seam, one `collectDelivery` point in run-node covering both the close's staging and the red-phase seal, `set-aside` journaled and documented); rejected any further tidying as scope the later checks own — next worker: `collectDelivery` in src/loop/run-node.ts is the single collection point, do not add a second staging path when the SARIF/friction artifacts land at settle.

#### ga.exec-stdout (c2)
- (RED) Tried: 2026-09-09 wrote the RED test test/integration/exec-stdout.test.ts (7 cases: stdout-only vs interleaved output, stderr-only, stdout-only, a JSON doc on stdout parsing through stderr noise, timeout, spawn-failure 127, UTF-8 decoder flush) and confirmed it fails only on the missing ExecResult.stdout — tsc reports it at 8 sites; rejected a `sh -c '…; sleep 30'` timeout case because killTree's process-group kill does not reach a grandchild (Bun.spawn makes no group leader), so it hung 30s — used `exec sleep 30` instead, and that tree-kill gap against exec.ts's own D1/D12 comment is real but out of scope here; nothing implemented, only the new file is in the tree.
- (IMPL) Tried: 2026-09-09 added `stdout: string` to ExecResult and filled it from the exec seam's own stdout drain (shared `keep()` append so `output`'s arrival-order interleaving and the decoder flush are unchanged), plus honest values at every construction site — `''` for spawn-failure and guardedExec's guard hit, the real stdout buffer for the real-process test helpers, output-mirrored for the umbel spies whose verbs answer on stdout; changed test/loop/harness.ts's `execScript` type to `{output, exitCode}` with the harness filling `stdout: r.output` rather than editing 39 call sites to a less honest `stdout: ''`; widened guardedExec to return ExecResult, but left `execGateWithRetry` in run-node.ts declaring the narrow struct (stdout survives it at runtime by reference — the gate node only needs to widen the annotation); `bun run check` green at 436 pass / 0 fail.
- (GREEN) Tried: 2026-09-09 GREEN phase found nothing red — `bun run check` exits 0 (tsc + biome clean, 436 pass / 0 fail), the evidence test and the pre-existing exec suite pass together 16/16, and `weeder check --strict` returns zero results on the diff; no code changed this phase, the nine files from the implement phase are the whole diff and `.loop-scratch/` is empty; next node need only widen `execGateWithRetry`'s declared return type in src/loop/run-node.ts to ExecResult, since stdout already flows through it by reference.

#### ga.parse-sarif (c3)
- (RED) Tried: 2026-09-09 c3 RED — wrote test/unit/sarif.test.ts (35 tests: accept real weeder SARIF with runs and suppressions intact, refuse non-JSON / non-object JSON / other versions / non-array runs, plus a hostile-corpus totality and purity group) and test/fixtures/weeder-check.sarif, a real `weeder check --strict --format sarif` log obtained by judging the repo against HEAD~40 with a Weeder-allow trailer so a genuine suppression appears; rejected shelling out to weeder from the test (unit tests do no processes) and rejected trimming the fixture's rules array without fixing `ruleIndex` (it would have been internally inconsistent). Test body validated by mutation against a scratch candidate in `.loop-scratch/sarif-candidate.ts` (left there for the green worker); the `Array.isArray` guard is an equivalent mutant because JSON arrays cannot carry `version`, so no test asserts it. Green must add only `src/core/sarif.ts` — full check is otherwise green (429 pass, 1 fail = the missing module).
- (IMPL) Tried: 2026-09-09 c3 GREEN — added src/core/sarif.ts, a hand-written total narrowing (catch-all try around JSON.parse, explicit exclusion of null and arrays, exact `version === '2.1.0'`, `Array.isArray(runs)`, fresh return literal so `$schema`/`__proto__` do not ride along); rejected zod (two facts to check, and audit-egress.ts is the house precedent for parsing untrusted text by hand), rejected copying `runs` defensively (each call re-parses, so nothing is shared), and rejected exporting parseSarif from src/index.ts (delivery/hygiene/receipt are internal too; the barrel is the plan-schema surface). Test and fixture untouched; `bun run check` green at 464 pass / 11 skip / 0 fail. The scratch candidate from the red phase is still at `.loop-scratch/sarif-candidate.ts` and is now superseded.
- (GREEN) Tried: 2026-09-09 c3 GREEN complete — `bun run check` exits 0 (464 pass / 11 skip / 0 fail, typecheck and biome clean) and `weeder check --strict` reports zero findings on the diff; the only change this phase was answering weeder's S2 on the empty `catch` with a comment stating why a parse failure is the answer rather than an error to report (rejected logging or wrapping it: core does no I/O and the caller's only question is whether a log exists). Module unchanged otherwise: hand-written narrowing over zod, `runs` left `unknown[]`, not exported from src/index.ts (delivery/hygiene/receipt are internal too). The superseded red-phase candidate at `.loop-scratch/sarif-candidate.ts` can be ignored.

#### ga.artifact-sha (c4)
- (RED) Tried: 2026-09-10 wrote the c4 RED at test/loop/gate-artifact-receipt.test.ts — five cases through runPlan on the in-memory harness (SARIF smoke → artifactSha = sha256 of the stdout bytes; the hash inside the sealed envelope with verifyReceipt pass/tampered; non-SARIF keeps nothing with the gate array pinned to today's canonical bytes; artifactSha:undefined re-hashes to the same seal; a setup gate printing SARIF keeps nothing). Extended harness.ts's execScript to optionally name `stdout` apart from the interleaved `output` (defaults to `output`, every existing script untouched) — without it stdout === output and an implementation hashing the interleaved stream would pass, an answer key. Rejected pinning the receipt's sha256 to a literal (durationMs is in the facts, so the envelope is not reproducible) in favour of pinning canonicalJson(facts.gates). tsc also reds on the missing GateRecord.artifactSha at three read sites; suite is 474 pass / 2 fail (both mine) over 11 runs — one earlier run reported a third failure that never reproduced and whose name I did not capture, worth watching.
- (IMPL) Tried: 2026-09-10 implemented c4 — `GateRecord.artifactSha?` in core/receipt.ts, and run-node's smoke gate hashing `stdout` (not the interleaved `output`) with `parseSarif` deciding, plus `RunNodeResult.smokeStdout` carrying the kept bytes for c5; `execGateWithRetry` now returns the whole `ExecResult` instead of narrowing `stdout` away. Chose parse-decides over exit-code-decides (a failing `weeder check --strict` writes the log that says why) and scoped the hash to the smoke gate only (one artifact per node — a setup gate printing SARIF keeps nothing; the committed test pins both). `smokeStdout` is set on the done return AND handBack and reset per attempt, so the bytes always match the final attempt's sealed hash — c5 must read it from both paths. `bun run check` green, 476 pass / 0 fail.
- (GREEN) Tried: 2026-09-10 GREEN pass — nothing red to fix: `bun run check` exits 0 (tsc + biome clean, 476 pass / 0 fail), the five c4 tests pass, and `weeder check --strict` returns an empty results array on the diff. Working tree is the two implementation files from the impl phase (`GateRecord.artifactSha?` in core/receipt.ts; run-node's smoke gate hashing `stdout` when `parseSarif` accepts it, plus `RunNodeResult.smokeStdout` carrying the kept bytes on both the done return and handBack). Nothing added this phase — no formatting needed since no new files, and I rejected touching the committed test to avoid moving the spec after the fact. Next worker (c5) reads `smokeStdout` from both hand-back paths.

#### ga.settle-write (c5)
- (RED) Tried: 2026-09-10 wrote the RED test for c5 (settle keeps the SARIF): 8 failing assertions covering write-before-dispose on both the done and quarantine paths, sha ↔ seal equality, `artifacts.sarif` outside the envelope, the single `gate-artifact` event, final-attempt-only on retry, non-SARIF keeping nothing, and a throwing store yielding `receipt-write-failed` + a normal close; harness gained `writeArtifact`, an `artifacts` map, a `receipt.artifact` log event and `writeArtifactThrows`. Rejected: casting the doc pins into the existing `test/unit/journal-doc.test.ts` only — the verifier runs one evidence file, so `docs/journal.md`'s `gate-artifact` row and the `CHANGES.md` note are asserted here too; also rejected asserting the real `<git-dir>` path from a loop test (that is c7's e2e job — the in-memory store mirrors the real store's naming instead).
- (IMPL) Tried: 2026-09-10 implemented c5 — `ReceiptStore.writeArtifact` + `ArtifactKind` in deps/seam, `Receipt.artifacts` outside the envelope, `keepArtifactsOrJournal` in run-plan wired before dispose on both the done and quarantine paths, `gate-artifact` documented and the `receipt-write-failed` row widened, dated additive note in CHANGES.md; `bun run check` green (485 pass). Took the journal's `sha256` from the minted receipt's gate record rather than recomputing it from the bytes, so the event can never disagree with the seal. Rejected keeping a log on the done path's commit-failure branch (it writes no receipt, so nothing would name the file) and rejected putting the path inside `facts` (it is settled after the freeze, like `refs`). Next worker: the `friction` kind and the `artifacts.friction` slot already exist for c6, unused — `keepArtifactsOrJournal` is where it hooks in.
- (GREEN) Tried: 2026-09-10 GREEN phase found nothing red — `bun run check` exits 0 (485 pass) and `weeder check --strict` reports zero findings against the diff; spent the pass re-reading the diff hostilely and made comment-only edits (moved `ArtifactKind` above the interface that uses it, removed the duplicated "the store owns the naming" line from the seam, reworded two clumsy sentences). Rejected touching any test or assertion, and rejected adding a friction-journal keep here — c6 owns it, and the `friction` kind plus the `artifacts.friction` slot are already in place for it, unused, with `keepArtifactsOrJournal` as its hook.

#### ga.friction (c6)
- (RED) Tried: 2026-09-10 RED for c6 — wrote test/loop/gate-artifact-friction.test.ts (8 tests, 7 red, 1 negative control green) plus a `plotplotByNode` fixture and a real `readFriction` in test/loop/harness.ts; asserted the friction journal is kept before dispose on both close and quarantine paths, hashed over the kept bytes, named outside the envelope, coexisting with a kept SARIF, and set aside from staging rather than staged. Rejected declaring `readFriction` in src/loop/deps.ts during RED (it would drag the real seam in and make the red structural, not behavioural) and rejected asserting the seam's own directory read here (it belongs in integration — src/seams/isolate.ts readFriction currently has no integration test, a gap the next worker should close). Note for GREEN: `keepArtifactsOrJournal` needs the `Isolation` passed in, and on the failed path `iso` is only in scope inside `if (iso)`; separately, `quarantineOrJournal` stages `changedFiles` without `partitionDelivery` — the #74 trap from the quarantine side, outside c6's claim.
- (IMPL) Tried: 2026-09-10 IMPLEMENT for c6 — added `IsolateSeam.readFriction` (deps.ts + a real `readdir`/`readFile` implementation in seams/isolate.ts) and made settle keep both artifacts through one `keepOrJournal` helper with a per-artifact try/catch, so a failed keep loses neither the other artifact nor the close; updated the `gate-artifact` row in docs/journal.md and the receipt entry in docs/contract/CHANGES.md to name the friction sidecar. Added an integration test for the seam's real directory read (the loop test only proves the in-memory seam, and shipping unexecuted I/O is how the D14 traps got in). Rejected a new journal event for a read failure (`receipt-write-failed` already covers "one of the artifacts kept beside it", and a new event costs a doc row for nothing) and rejected reading friction through git (it is deliberately ignored/untracked, so only a plain fs read sees it). Next worker: on the close path a commit/emit failure still keeps nothing, friction included — pre-existing for the SARIF, but the journal's value does not depend on a receipt existing, so the e2e node should decide whether that stands; and `quarantineOrJournal` still stages `changedFiles` without `partitionDelivery` (the #74 trap from the quarantine side).
- (GREEN) Tried: 2026-09-10 GREEN for c6 — ran `bun run check` (exit 0: typecheck, biome, 494 pass / 0 fail) and `weeder check --strict` (exit 0, no findings); nothing was red, so no code changed this phase and the evidence test still matches the sealed red phase byte for byte. Next worker: the two open items from IMPLEMENT stand — a commit/emit failure on the close path keeps no artifacts at all (friction included, since no receipt exists to name them), and `quarantineOrJournal` still stages `changedFiles` without `partitionDelivery`, which is the #74 trap from the quarantine side.

#### ga.e2e (c7)
- (RED, attempt 1 (refused: already green)) Tried: 2026-09-10 wrote test/e2e/gate-artifact.test.ts (real CLI + real git, {command} nodes, SARIF/plain/weeder-skipIf/large-multi-byte cases) plus test/fixtures/sarif-smoke.sh and plain-smoke.sh; it passes on arrival because all four sibling nodes are already in this node's base, so the sink's RED phase is structurally unreachable — rejected faking a failure or weakening the test, and wrote BLOCKED.md instead; probed the quarantine path (SARIF smoke exiting 1 — log still kept) and a 2.8 MB multi-byte log (bytes verbatim, seal matches) to confirm no real gap; next worker: shape a loop's final end-to-end verification node as {command}/{prompt} work, not phases, and record that emit-plan gap in docs/dogfood/gate-artifacts.md.
- (RED, attempt 2) Tried: 2026-09-10 Rejected the previous attempt's BLOCKED.md (it claimed a sink node has no reachable red phase); the siblings are green in isolation but leak at the seam — probed the real CLI and found that an acceptance-evolution re-close leaves the earlier close's `<node>.sarif` and `<node>.friction.jsonl` beside a receipt that seals neither, so RED is `test/e2e/gate-artifact.test.ts > a re-closed node keeps only what its own close sealed`; the GREEN fix is in `keepOrJournal` (drop a stale artifact of a kind this close keeps nothing for, both kinds, journal a failure and never block the close).
- (IMPL) Tried: 2026-09-10 Implemented the discard the RED case demanded — `ReceiptStore.discardArtifact(node, kind)` (real store unlinks, ENOENT is the answer, other errno propagates to the existing receipt-write-failed journal; harness store deletes from its map), called from `keepOrJournal` whenever a kind keeps nothing, so both kinds and both settle paths (close and quarantine) are covered; rejected keying artifacts by receipt sha or leaving the stale file for a reader to date-check, since the contract pins `<node>.sarif` and an orphan log under the node's name is the defect; next worker should know the discard is deliberately silent (no journal line for the overwhelmingly common nothing-to-forget case) and that a skipped, still-verified node never reaches settle, so its artifacts correctly stay.
- (GREEN) Tried: 2026-09-10 GREEN phase — ran `bun run check` (exit 0, 498 pass / 11 skip / 0 fail) and `weeder check --strict` (exit 0, no results) with no further fixes needed; confirmed the skipIf weeder case actually ran here against the real binary rather than skipping, so all four e2e cases are exercised on a machine with weeder and only that one skips on CI; nothing was weakened and no sibling check reads red, so the loop's implementation stands as the discard added in the previous phase.
