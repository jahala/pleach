# Dogfood — teardown-keeps-work (D16, jahala/pleach#61 #67 #72)

What the pleach agent hit in tend2, pleach, umbel, weeder and pollen while conducting this loop.
The shaping-pass findings that apply to every loop of the 2026-09-08 order are recorded once in
`test-phase-commit.md`; this file carries what this loop's run added.

## Conducting (2026-09-11)

Run: `pleach run docs/dogfood/teardown-keeps-work/plan.json --repo-root . --max-concurrency 1`, the
one agent running, workers claude-opus-5 through umbel, smoke `weeder check --strict`, audit = tend2
verify on the node's check as a self-integral audit run by opencode + deepseek/deepseek-v4-pro.
Result: 7 of 7 nodes verified in 7 attempts — the first loop with no retry at all; landed by
`pleach land` (fast-forward, land gate green) onto feat/teardown-keeps-work at 6e3be19;
`bun run check` green (571 tests); every check stamped by `tend2 verify`.

| node | check | attempts | wall |
|---|---|---|---|
| tk.adapter-abort | c1 | 1 | 7m36s |
| tk.settle-aborted | c2 | 1 | 10m14s |
| tk.drain | c3 | 1 | 8m45s |
| tk.stop-face | c4 | 1 | 6m16s |
| tk.idle | c5 | 1 | 14m19s |
| tk.docs | c6 | 1 | 9m21s |
| tk.e2e | c7 | 1 | 19m41s |

### Findings, per node
### tk.adapter-abort (verified, 1 attempt(s))
- 7m36s, audit pass. The adapter checks waitOpts.signal.aborted BEFORE the non-zero-exit check and returns gatherWorkerResult(…, 'aborted') — nothing gathered from the killed tree, sinceMtime left as the send set it. A fake-claude-wedged.sh fixture proves it against the real umbel binary; the non-signal non-zero exit still throws. Reviewed.

### tk.settle-aborted (verified, 1 attempt, 10m14s, audit pass)
- run-node hands the live tree back with status 'aborted' + gate.ran 'wait:aborted' for a build wait AND an audit wait the signal cut off; RunSummary.aborted (additive), summaryExitCode counts it, run-end doc row updated; the finally now only disposes what an early or thrown path still holds. Reviewed.

### tk.drain (verified, 1 attempt, 8m45s, audit pass)
- LockSeam.stopRequested/clearStop; the marker is `<lock>.stop`, presence is the request (one stat per launch decision); read fresh per launch, never cached; one authoritative read at run-end journals run-stopped and consumes the marker (an unconsumed marker would drain the NEXT run); KINDS pins run-stopped; narrate has a line. Reviewed.

### tk.stop-face (verified, 1 attempt, 6m16s, audit pass)
- `pleach stop <plan> [--now]`: requestStop/signalRun live in the lock seam (the face never signals); a dead pid is NoRunError → exit 3 and NO marker written (a marker no run reads is a trap for the next one); help text and exit-code table updated; hold-lock.ts fixture holds a real lock from a child process. Reviewed.

### tk.idle (verified, 1 attempt, 14m19s, audit pass)
- Worker.wait gains idleMs (informative seam prose + CHANGES.md, no schema change); `--idle-ms` default 10 min at the face, threaded through RunPlanOpts → RunNodeOpts → every wait (build AND audit); the umbel adapter appends --idle-timeout only when given; fake-claude-idle.sh proves `idle` from the real binary and a blocked settle with quarantine. Reviewed.

### tk.docs (verified, 1 attempt, 9m21s, audit pass)
- The verdict row now names the status vocabulary (+aborted); README gains `pleach stop` and a "Halting a run never loses a node's work" paragraph; the pin test extended to assert the statuses and run-end fields the doc names. run-stopped's row was already there from tk.drain (its GREEN phase had to satisfy the pin). Reviewed.

### tk.e2e (verified, 1 attempt, 19m41s, whole-page audit 7/7 pass)
- Two real findings from the real-CLI proof. (1) A {command} node spawns no worker, so its exec IS its wait — and the teardown signal never reached it: an aborted command read as a failed gate and was retried. run-work now passes the signal to the command exec and answers `aborted` when its own signal fired, the same answer the umbel adapter gives. (2) The exec seam spawned children in pleach's own process group, so `kill(-pid)` named no group: the command died but the `sleep` it forked lived on holding the pipe, and the drain waited out the very work it was tearing down. Children now lead their own group (`detached: true`), which also means a terminal's Ctrl-C reaches pleach alone — it quarantines and records — instead of felling every gate command behind its back.

### Tools, this run
- pleach: this loop's own subject. Before it, a SIGINT during an audit had disposed a finished build (#67, twice); the e2e now proves the aborted receipt, the quarantine and the drain from a second process.
- umbel: `wait --json --idle-timeout` answers `{"reason":"idle"}` with exit 0, as the adapter assumed; no new findings. The fake-worker fixtures (wedged, idle) are the pattern for proving runner behaviour without a model.
- tend2, weeder, pollen: nothing new. `weeder check --strict` green on all seven; its SARIF kept beside each receipt.

### Issues touched from this loop
- pleach #61 (the stop half; the resume-from-quarantine half is #91, "Nothing is lost"), #67, #72 — closed with the merge commit.

## Tried lines from the workers' handbacks (every phase, verbatim)

One line per node — the implementation phase's — is transcribed onto the page, marked as
transcribed. Every phase's line is kept here. Recovered from Claude Code's own transcripts
(jahala/pleach#66).

#### tk.adapter-abort (c1)
- (RED) Tried: 2026-09-10 wrote the RED integration test for c1 (adapter `wait` → `aborted` on the run's signal) plus a wedged fake-worker fixture; probed the real umbel binary first and rejected two obvious negative-case vehicles — a bogus session name returns `{"reason":"dead"}` exit 0, and `timeoutMs: NaN` degenerates into an exec-timeout kill with a Node warning, so the negative uses `--timeout -1ms` (real exit 1); the suite resolves the binary via `PLEACH_UMBEL_BIN ?? Bun.which('umbel')` because the env-var-only ladder in `umbel-seam.test.ts` would skip and make the check vacuous — the next worker should not narrow that back.
- (IMPL) Tried: 2026-09-10 implemented c1 as an eleven-line early return in `src/adapters/umbel.ts` wait() — `signal.aborted` → `gatherWorkerResult(name, cwd, 'aborted', undefined)` before the non-zero-exit check; reused gatherWorkerResult rather than inlining the WorkerResult literal (its non-stop branch is already that shape and spawns no exec calls), keyed the branch on the signal rather than on exit 137 so the whole kill-code class is covered, and deliberately let the early return skip the `sinceMtime` reset so an aborted wait keeps the send's baseline for a later wait; `bun run check` green, 536 pass.
- (GREEN) Tried: 2026-09-10 GREEN phase for c1 — ran `bun run check` (exit 0: tsc, biome, 536 pass/11 skip/0 fail) and `weeder check --strict` (exit 0, zero results) with no fixes needed; left the diff at the eleven-line `signal.aborted` early return in `src/adapters/umbel.ts` and did not touch the RED test or fixture; declined to run `tend2 verify` myself since the loop page's check states belong to the verifier, and left the pre-existing `umbel-pl-9616a3f4` tmux session alone because it predates this node and is the conductor's own worker.

#### tk.settle-aborted (c2)
- (RED) Tried: 2026-09-10 wrote the RED test/loop/abort-settles.test.ts — build-wait abort and audit-wait abort, both settling `aborted` with receipt + quarantine + summary bucket + unclean exit; verified against a scratch probe that every non-status assertion (gate.ran wait:aborted, single spawn, derived quarantined, quarantine/x staged with both changed files before dispose, run-end line) already holds today, so the red is behaviour and nothing else. Rejected asserting `summary.aborted` through a typed cast — `toMatchObject` keeps tsc green so the conductor's RED is a test failure, not a compile error; rejected pinning the audit gate string exactly (it is `<command> (auditor aborted)` and the work order only requires the status to change there). GREEN must also move abort.test.ts's `summary.failed === ['a']` to the aborted bucket and add `aborted: []` to cli-summary.test.ts's RunSummary factory.
- (IMPL) Tried: 2026-09-10 implemented c2 — run-node hands back `status:'aborted'` with `gate.ran:'wait:aborted'` for an aborted build wait and for an aborted auditor, reusing the `baseVerdict`-spread idiom the blocked/dead branches already use rather than adding a `terminalVerdict` helper (5 untouched `failedVerdict` call sites would have paid for 2); `RunSummary.aborted` added with settle routing to it and `summaryExitCode` counting it unclean. Beyond the test: added `aborted` to `ready()`'s guard, not just the `skipped` filter — the target test can't catch that gap because its abort also sets `signal.aborted`, which is the only thing stopping the scheduler from relaunching a settled aborted node; `pleach stop`'s drain (c3) has no such signal, so without it a stopped run would relaunch its own aborted nodes. Also hoisted `result.reason ?? 'aborted'` so the gate string stops reading `wait:undefined` for an unnamed abnormal end. Next worker: c1 (the umbel adapter) is what makes this reachable on the real seam — today only the in-memory runner returns `aborted`; and docs/journal.md's run-end row now names the field, but the `aborted` verdict status and `run-stopped` prose still belong to c6.
- (GREEN) Tried: 2026-09-10 GREEN phase — ran `bun run check` (exit 0: tsc, biome, 539 pass/0 fail) and `weeder check --strict` (exitCode 0, no results); no red to fix, since c2's implementation landed in the IMPLEMENT phase, so I changed nothing rather than manufacture work. Re-confirmed the evidence file and both dependent tests pass in isolation and that the tree holds only the eight files the change needs. Next worker: c1 (umbel adapter returning `aborted` instead of throwing) is what makes this path reachable off the harness; the `aborted` verdict status and `run-stopped` prose in docs/journal.md still belong to c6 — I added only the run-end row's field.

#### tk.drain (c3)
- (RED) Tried: 2026-09-10 c3 RED — wrote test/loop/stop.test.ts (four cases: marker on a close, marker before the first launch, marker while two nodes are in flight, and the no-marker control) and gave the harness lock a stop-marker flag exposed as `Harness.stop`. Three fail on the missing scheduler read, one passes as the control; full suite otherwise green, tsc and biome clean. Rejected: annotating the harness lock literal as `LockSeam` (excess-property check would have made RED a compile error) and flipping the marker from a's waitScript in the chain case — flipping it in `emitDecision` lands it inside the close itself, which is the #67 race; the waitScript flip is used only where the point is a node still mid-turn. Next worker: `run-plan.ts` must evaluate `stopRequested` per launch decision (not once per outer tick) or case 2 stays red, and must not break out of the loop while `inflight` is non-empty or case 3 stays red.
- (IMPL) Tried: 2026-09-10 c3 IMPLEMENT — added `stopRequested`/`clearStop` to LockSeam, implemented them in the real seam over `<lockfile>.stop`, put the read into the scheduler's launch decision plus one authoritative end-of-loop read that journals `run-stopped` and consumes the marker, and pinned `run-stopped` in KINDS + docs/journal.md + narrate (the journal-doc test and the envelope both require it, so it is not scope creep). Rejected: caching the marker in a `stopped` flag set inside the loop (a stop landing after the last launch decision would then never be consumed and would drain the next run); reading it before `ready().find()` (a stat on every idle tick for no decision); adding a `requestStop` writer (that is c4's, and there is nothing honest to test it with here). Next worker: `stopPath()` is exported from `src/seams/lock.ts` for the CLI's writer, and note `stopRequested` deliberately re-throws non-ENOENT rather than answering false.
- (GREEN) Tried: 2026-09-10 c3 GREEN — verified `bun run check` exits 0 (543 pass, 0 fail, biome and tsc clean) with the c3 implementation in place and re-audited the six-file diff; confirmed the lock cannot leak on a `stopRequested` throw (release is in `runPlan`'s `finally`) and that only the real seam and the harness implement `LockSeam`, both complete under a tsconfig that includes `test`. Rejected: making a non-ENOENT marker-read answer `false` (that launches through an operator's stop — defect #67) and adding a `requestStop` writer here (it belongs to c4, which is why `stopPath()` is exported). Next worker: nothing writes the marker yet, so c4 owns both the CLI verb and the real seam's write path.

#### tk.stop-face (c4)
- (RED) Tried: 2026-09-11 wrote the RED integration test for `pleach stop` (marker beside the lock, exit 3 without a live holder, `--now` → SIGINT to the lock's pid) plus a real lock-holding child fixture at test/fixtures/hold-lock.ts; rejected planting a stale lockfile by hand-computing the sha1 path (lock.test.ts does that, but SIGKILLing a real holder proves "stale is not live" without duplicating the seam's path knowledge) and rejected calling runCli in-process (every CLI test here spawns src/main.ts, and in-process writes would pollute the suite's output); next worker must know the marker is asserted on the `--now` path as well, and that the seam already owns stopPath/stopRequested/clearStop — only requestStop and signalRun are missing.
- (IMPL) Tried: 2026-09-11 implemented `pleach stop` — `NoRunError` in core/errors.ts, `requestStop`/`signalRun` over a private `liveHolder` in src/seams/lock.ts, the verb plus `--now` and help in src/faces/cli.ts; rejected adding requestStop/signalRun to the `LockSeam` interface in loop/deps.ts (the loop only reads and clears the marker, so the port would gain two methods test/loop/harness.ts must implement and nothing would call) and rejected writing provenance bytes into the marker (stopPath's invariant is that presence is the whole request, keeping the scheduler's per-launch check a single stat); next worker must know a stale lockfile refuses with exit 3 like an absent one, `--now` writes the marker before signalling so a racing run still sees the drain, and `--now` is now globally parseable on every verb though only `stop` reads it.
- (GREEN) Tried: 2026-09-11 ran `bun run check` on the finished `pleach stop` implementation and confirmed it exits 0 with no formatting or lint fixes needed, so nothing required repair in this phase; reviewed the working-tree diff against the weeder rules and found it limited to the three source files (NoRunError, requestStop/signalRun over a private liveHolder, the verb plus --now and help), with no test edits, deletions, or stubs; next worker must know `--now` writes the marker before signalling so a racing run still drains, a stale lockfile refuses with exit 3 exactly like an absent one, and `--now` parses on every verb although only `stop` reads it.

#### tk.idle (c5)
- (RED) Tried: 2026-09-10 wrote the RED for c5 — test/integration/umbel-idle.test.ts (adapter idle + a runPlan-over-real-umbel proof that a quiet worker settles blocked with quarantine/<id> holding its file), argv assertions in test/unit/umbel-seam.test.ts, and a new test/fixtures/fake-claude-idle.sh (writes a file, prints once, then silent). Confirmed failing for the missing behaviour (rides the 60s attempt clock; no --idle-timeout in the argv) plus tsc red on the absent idleMs; probed the real binary first — `--idle-timeout` accepts `2000ms` and returns exit 0 `{"reason":"idle"}`, and without the flag the same worker returns `timeout`, which is kept as the negative case. Rejected reusing fake-claude-wedged.sh (writes nothing, so there is no tree to quarantine) and rejected casting the opts object to dodge the type error — the missing `idleMs` on Worker.wait/RunPlanOpts is part of the spec being proven.
- (IMPL) Tried: 2026-09-10 implemented c5 — `idleMs` on `Worker.wait` opts, threaded RunPlanOpts → RunNodeOpts → runWork/runAudit so all three wait sites (both build waits and the auditor's) carry it, `--idle-timeout ${idleMs}ms` in the umbel adapter, `--idle-ms` (default 600000) in the CLI help, informative seam block + a doc-only CHANGES entry. Rejected giving the adapter its own fallback idle default (the policy belongs to the conductor; the unit test pins "no idleMs, no flag") and rejected adding a fourth positional to `runAudit` in favour of a `{ signal, idleMs }` group. Next worker: `umbel wait --idle-timeout` accepts ms units (`2000ms`), and the exec headroom of `timeoutMs + 10s` is unchanged — an idleMs larger than timeoutMs simply loses to umbel's hard deadline.
- (GREEN) Tried: 2026-09-10 green phase for c5 — ran `bun run check` (exit 0: tsc + biome clean, 552 pass / 0 fail), `bun test test/integration/umbel-idle.test.ts` (3 pass, 14s) and `weeder check --strict` (exit 0, no findings); no red to fix, so nothing was changed in this phase and the implement-phase diff stands untouched. Rejected touching the loop page or check states (verifier's, read-only here). Next worker: c6's README mention of `--idle-ms` is still outstanding — the CLI help side is done.

#### tk.docs (c6)
- (RED) Tried: 2026-09-10 RED for c6 — extended test/unit/journal-doc.test.ts with a verdict-status vocabulary pin (against VerdictSchema, not a literal list), a run-end pin against RunSummary's buckets, a help pin against the verbs/flags cli.ts really dispatches and parses, and a README usage-block pin against those verbs; three fail (verdict row states no statuses; README lacks stop/receipt/clean and never names `pleach stop` or `--idle-ms`). Rejected a plain `toContain('run-stopped')` assertion — the existing source-scan pins already force any appended event to be documented and kinded, so it would have been a duplicate green; also rejected spawning the CLI for `--help` in a unit test (that shape belongs to test/e2e/help.test.ts, which stays green) and read the HELP literal out of the source instead. Next worker: the README fix needs three verb lines (stop, receipt, clean) plus a mention of `--idle-ms`, and the verdict row must list all seven statuses with no trailing `…` — the parser demands an exact match.
- (IMPL) Tried: 2026-09-10 IMPLEMENT for c6 — added the status vocabulary to docs/journal.md's `verdict` row (pinned to VerdictSchema, gloss moved to the meaning column after a first attempt put `aborted` in the parenthetical twice and failed the scan) and brought README's usage block up to the CLI's verb set (stop, receipt, clean) with a halting paragraph naming `pleach stop`, `--now` and `--idle-ms`; no src change was needed because run-stopped/KINDS/CLI help already landed in the sibling nodes, so I rejected touching journal-envelope.ts and cli.ts as make-work; `bun run check` green (558 pass, 11 skip), test/e2e/help.test.ts untouched and still green.
- (GREEN) Tried: 2026-09-10 GREEN for c6 — verified the phase came in already green (`bun run check` exit 0, weeder --strict exit 0 on the two-file doc diff), so I changed nothing further and instead audited the README paragraph's claims against source rather than the loop page: 10m idle default in cli.ts, the marker read inside the launch loop in run-plan.ts, receipt+quarantine for aborted in abort-settles.test.ts — all hold; rejected touching src/core/journal-envelope.ts and src/faces/cli.ts since the siblings had already landed `run-stopped` in KINDS and `--idle-ms`/`pleach stop` in the help. Next worker: the vocabulary pin reads the first parenthetical after `` `status` `` and treats every backticked token in it as a status, so explanatory asides must go in the meaning column.

#### tk.e2e (c7)
- (RED) Tried: 2026-09-10 wrote the RED sink test at test/e2e/teardown.test.ts — two real-CLI cases over {command} nodes (no runner, ungated): SIGINT mid-node (red: signal never reaches command work, node closes clean, exit 0) and `pleach stop` from a second process (green already, via the sibling nodes). Rejected polling for `node/<first>` before issuing the stop — the scheduler launches the dependent in the same tick as the close, so a second bun process can never win that race; the stop is issued mid-flight instead, with the same closed/skipped/run-stopped/marker-gone assertions. Next worker: threading `opts.signal` into `guardedExec` (src/loop/run-work.ts) is not enough — the interrupted exec then throws `GateFailedError('command', …)` and settles **failed**; the command path must distinguish the run's own signal and hand the live tree back with `status: 'aborted'` like the wait path at src/loop/run-node.ts:283.
- (IMPL) Tried: 2026-09-11 made c7 green with two changes — `src/loop/run-work.ts` threads the teardown signal into {command} work and answers `reason: 'aborted'` when its own signal fired (umbel adapter's precedent, so run-node's existing D16 hand-back does receipt + quarantine), and `src/seams/exec.ts` spawns children `detached: true`. The second was not optional: `Bun.spawn` children inherit the parent's process group, so `killTree`'s `kill(-pid)` named no group, killed only the direct child, and left the grandchild holding the stdout pipe — measured 15011ms to abort a 15s sleep, versus 502ms once the child leads its own group. Rejected the symptom fix of racing `drainDone` against the interrupt (would return early but leak the orphan and leave the seam's stated "reap the process tree" still a lie), and rejected inventing a `command:aborted` gate string (the sibling tests pin `wait:aborted`; one vocabulary for "the run's signal ended this"). Gate execs deliberately still run to completion on their own timeouts. Next worker: exec children are now process-group leaders and no longer die from a terminal's group-wide Ctrl-C — teardown is the seam's alone, which is what makes the quarantine deterministic; the full suite got ~15s faster because the timeout paths were paying the same orphaned-pipe tax.
- (GREEN) Tried: 2026-09-11 GREEN phase found nothing red — re-ran `bun run check` (exit 0; 560 pass, 11 skip, 0 fail), `weeder check --strict` (exit 0, zero results), and as the loop's sink re-ran every check's evidence on the page together (32 tests across all 7 files, 0 fail, 0 skip), so no sibling needed fixing here. Confirmed biome needs no formatting pass on the new test since `lint` is scoped to `src test` and the file is inside it. The 11 suite skips are pre-existing environment gates (umbel-seam tmux fixtures, the three missoula module-transport tests, the full-stack e2e) and belong to no check on this page. Diff unchanged from the implement phase: the signal threading in run-work.ts plus `detached: true` in the exec seam.
