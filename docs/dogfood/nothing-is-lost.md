# Dogfood — nothing-is-lost (D17, jahala/pleach#66 #82 #65 #77 #91)

What the pleach agent hit in tend2, pleach, umbel, weeder and pollen while conducting this loop.
The shaping-pass findings that apply to every loop of the 2026-09-08 order are recorded once in
`test-phase-commit.md`; this file carries what this loop's run added.

## Conducting (2026-09-11)

Run: `pleach run docs/dogfood/nothing-is-lost/plan.json --repo-root . --max-concurrency 1`, the
second agent beside bandung, workers claude-opus-5 through umbel, smoke `weeder check --strict`,
audit = tend2 verify on the node's check as a self-integral audit run by opencode +
deepseek/deepseek-v4-pro. Result: 7 of 7 nodes verified in 7 attempts; landed by `pleach land`
(fast-forward, land gate green) onto feat/nothing-is-lost at db0a628; `bun run check` green (619
tests); every check stamped by `tend2 verify`.

Conducted under D13, D14 and D16. From the first node's close on, every close on this loop kept
its own handback beside its receipt — the Tried lines below were the first ever recovered from
pleach's own store rather than from Claude Code's transcript.

| node | check | attempts | wall |
|---|---|---|---|
| nl.handback | c1 | 1 | 15m08s |
| nl.receipt-history | c2 | 1 | 21m03s |
| nl.fallback | c3 | 1 | 17m15s |
| nl.audit-verb | c4 | 1 | 37m49s |
| nl.resume | c5 | 1 | 25m47s |
| nl.docs | c6 | 1 | 7m01s |
| nl.e2e | c7 | 1 | 52m25s |

### Findings, per node
### nl.handback (verified, 1 attempt, 15m08s, audit pass)
- ArtifactKind 'handback' (.handback.md); RunNodeResult.handback = the BUILD worker's finalMessage (never the auditor's — that is egress the loop parses); kept before dispose through keepOrJournal with gate 'handback'; receipt.artifacts.handback; an empty message keeps nothing. Reviewed. From this node on, every close on this loop keeps its own Tried line — #66 closes with the merge.

### nl.receipt-history (verified, 1 attempt, 21m03s, audit pass)
- The store files every close as <node>.<12-hex prefix>.json beside <node>.json (latest); artifacts carry the close's prefix so a kept SARIF is never the wrong close's; ReceiptStore.readAt; receipt-verify walks refs.previousReceiptSha256 with a cycle guard and reports a missing link as the gap it is; the verb prints the history. Reviewed. #82 closes with the merge.

### nl.fallback (verified, 1 attempt, 17m15s, audit pass)
- `--fallbackProvider` conductor-level; a `dead` attempt re-casts to it (a model pin goes with the dead provider); no fallback or the same provider → the attempt is left UNSPENT with the reason on the verdict's journal line; a fallback that is the auditor is refused (diversity re-checked). Smoke-red retries unchanged. Reviewed. #65 closes with the merge. Handbacks are now being kept: two .handback.md files beside receipts already.

### nl.audit-verb (verified, 1 attempt, 37m49s, audit pass)
- New src/loop/audit-node.ts (`pleach audit <plan> <node>`): refuses unless the node declares an audit and is quarantined with a green smoke; isolates from quarantine/<id>; re-runs only the audit ladder (tamper rule + runAudit, shared with run-node, not duplicated); pass → commit-before-emit to node/<id> with a receipt whose facts.base records the quarantine sha; refusal → a new quarantine receipt; AuditRefusedError → exit 1. audit-egress-unparseable now names the expected block. The longest node of the week (38 min): a new verb with its own loop file plus extracting the audit/settle code the two paths share. Reviewed the seams; behaviour pinned by the full suite. #77 closes with the merge.

### nl.resume (verified, 1 attempt, 25m47s, audit pass)
- baseRefsFor puts the quarantine sha first (checkout) and merges the closed needs; seeded ONLY when the node's latest receipt's refs.quarantineSha equals the branch (a superseded quarantine is refused with a journaled detail — standing on it would resurrect work a later close judged); `resumed-from-quarantine` {node, sha}; facts.base sealed; the first prompt carries the quarantine's diff stat as evidence (through the isolate seam); `--fresh` opts out. Reviewed. #91 closes with the merge.

### nl.docs (verified, 1 attempt, audit pass)
- docs/journal.md: resumed-from-quarantine row, handback in the gate-artifact row; KINDS pinned; README and the help block list `pleach audit`, `--fallback-provider`, `--fresh`. Reviewed.

### nl.e2e (verified, 1 attempt, 52m25s, whole-page audit 7/7 pass)
- The real-CLI proof found a gap the loop tests could not: a resumed PHASED tree already carries its red seal, and a resume that re-entered the ladder at red would either seal a lie (impl in the red) or seal nothing. The close that quarantines a tree now records `facts.redSealedAt` (sealed inside the envelope, like `base`), a re-adjudication carries it, and a resume re-enters the phase ladder after it. The longest node of the week (52 min, one attempt): two real-CLI scenarios and a scripted runner with an auditor that relays garbage once.

### Tools, this run
- pleach: the receipts directory now holds, per close, `<node>.<prefix>.json` plus `.sarif` and `.handback.md` — a node that ran twice keeps both stories. `pleach receipt <node>` lists the history. The handback made #66's workaround (reading ~/.claude/projects transcripts) unnecessary from this loop on.
- umbel, tend2, weeder, pollen: nothing new. `weeder check --strict` green on all seven.

### Issues touched from this loop
- pleach #66, #82, #65, #77, #91 — closed with the merge commit.

## Tried lines from the workers' handbacks (every phase, verbatim)

One line per node — the implementation phase's — is transcribed onto the page, marked as
transcribed. Every phase's line is kept here; from this loop on they are also kept by pleach
itself beside each receipt.

#### nl.handback (c1)
- (RED) Tried: 2026-09-10 wrote the RED for c1 (test/loop/handback-kept.test.ts, 10 cases: done/quarantine/aborted paths, final-attempt-only, builder-not-auditor, side-by-side with sarif+friction, empty discards a stale keep, write failure journals and the close stands) — confirmed failing on the missing keep with the setup assertions passing, tsc + biome clean, no source touched. Rejected: asserting a fixed order among the three `gate-artifact` events (the check does not fix one — asserted the sorted set instead), and asserting the handback through a typed `Receipt['artifacts'].handback` (the field does not exist yet, so the test casts a `KeptPaths` view to keep tsc green while the behaviour is red). Next worker: the empty-handback case runs the same node id twice on one harness, so the discard must go through `discardArtifact` like the SARIF path, and the aborted case proves `finalMessage` must ride every hand-back, not only `done`.
- (IMPL) Tried: 2026-09-10 implemented c1 — ArtifactKind 'handback' → `.handback.md` in the real store and the harness (harness now types off ArtifactKind, no second literal union), `RunNodeResult.handback` set from the work's final message once per attempt so it rides every hand-back and the done return, kept in run-plan through the existing keepOrJournal with gate 'handback' and sha256Hex(bytes), `Receipt.artifacts.handback` + a "Receipt, not schema" CHANGES.md entry; scoped three D14 assertions to their own artifact kind (they read every gate-artifact event and asserted an empty artifact store, which a third kept kind now breaks) and fixed two of my own RED scripts (the smoke gate's flake retry runs the command twice per attempt; the harness's abort race discards a scripted result). Rejected: special-casing `{command}` nodes out of the keep — their exec output is what that work handed back, and an extra branch buys nothing the claim asks for; reading anything inside the message (agents produce, code decides). Next worker: a close that fails at the late-marker/commit step still writes no receipt and therefore keeps no handback (pre-existing D14/D11 shape, and it disposes without quarantining) — if D17 wants that hole closed it needs its own claim; and the real umbel adapter returns `finalMessage: ''` for any non-stop wait, so a halted node keeps nothing until the adapter reports its capture.
- (GREEN) Tried: 2026-09-10 verified the GREEN — `bun run check` exit 0 (570 pass, 0 fail), `weeder check --strict` clean, c1's evidence 10/10; re-read the D14 test edits against the weakened-assertion bar and kept them exact-count and per-kind rather than relaxing them. Rejected: nothing new this phase (no red to fix). Next worker: a close that fails at the late-marker/commit step still writes no receipt and so keeps no handback (pre-existing D14/D11 shape — it disposes without quarantining); and the real umbel adapter returns `finalMessage: ''` for any non-stop wait, so a halted node keeps nothing until the adapter reports its capture.

#### nl.receipt-history (c2)
- (RED) Tried: 2026-09-10 wrote the c2 RED test (6 cases: per-close receipt files, per-close artifacts with the un-prefixed name as latest, a discard that does not take an earlier close's log, the verb's history walk, an empty history, a lost history file, and the CLI's printed lines) against the real store + real git + the real `pleach receipt` process; rejected pinning the sha-prefix length or the CLI's exact line format (the store owns the names — the test derives the prefix from the filename and checks it against the receipt hash), and rejected adding a `readAt` assertion to the test so the GREEN phase can choose its own read-by-sha shape. Note for the next worker: tsc is red before the fix (4-arg `writeArtifact`/3-arg `discardArtifact`, `ReceiptCheck.history`) — that is the API the test specifies, deliberately left uncast so no cast has to be removed later.
- (IMPL) Tried: 2026-09-10 implemented c2 — the store keeps every close (`<node>.<prefix>.json` + latest, `readAt`), artifacts follow the same two names, and the receipt verb walks `previousReceiptSha256` into a `history` the CLI prints one line per prior close; chose to have `writeArtifact` return the CLOSE'S OWN path (so a receipt names the artifact its own seal answers) and rejected returning the un-prefixed latest, which would have kept the D14 test literals untouched but left every historical receipt pointing at the newest close's log — the exact defect D17 names; that choice moved the pinned paths in gate-artifact-settle/friction, handback-kept and e2e/gate-artifact to paths derived from the close's receipt hash (no assertion weakened, one added), so those checks re-verify by re-running. Next worker: the 12-hex prefix lives once in `receiptPrefix()` in core/receipt.ts — derive names from it rather than slicing by hand, and `docs/journal.md`'s `gate-artifact` gate list still omits `handback` (c6's).
- (GREEN) Tried: 2026-09-10 GREEN phase — `bun run check` was already 0 from the implement phase, so the only work was weeder's two S2 findings on the catches in `readReceiptFile`: added the rationale at each catch (null is the honest "no record" answer both callers act on) rather than restructuring the reads or letting them throw, which would fail a close over a file the store only reads; weeder --strict is now clean and nothing else in the diff changed. Next worker: the 12-hex prefix has one home (`receiptPrefix()` in src/core/receipt.ts) — derive names from it, never slice by hand; and `docs/journal.md`'s `gate-artifact` gate list still omits `handback`, which belongs to c6's doc check.

#### nl.fallback (c3)
- (RED) Tried: 2026-09-10 wrote test/loop/fallback-provider.test.ts RED (8 tests, 5 failing on missing behaviour; tsc + biome clean, rest of the suite green). Spec'd through runPlan, not runNode, so the RunPlanOpts→RunNodeOpts threading and the verdict journal line are both covered; spawn providers are observed by wrapping deps.runner.spawnWorker (the harness logs role, not provider). Rejected throwing PlanInvalidError for the fallback/auditor collision — run-node's finally disposes the tree and the work would be lost, so it settles failed with a hand-back and the preflight's wording. Next worker must know: the fallback comparison is against the RESOLVED provider (a test leaves the plan silent and sets the fallback to 'claude'); the detail string on the verdict's journal line is pinned exactly for the no-fallback case; and run-plan still attributes the verdict to node.worker.provider, so a fallback attempt lies to the casting join (G1) — untested here, needs its own check.
- (IMPL) Tried: 2026-09-10 implemented c3 green — run-node gained `opts.fallbackProvider` and a mutable cast, the dead+resume branch re-casts on the fallback with `auditDiversityRefusal` re-asked against it (refusal settles failed, tree kept), and settles dead with `verdictDetail` when there is no usable fallback; run-plan threads the option and puts the reason on the verdict journal line; the CLI parses and advertises `--fallback-provider`. Rejected carrying `worker.model` into a fallback spawn (a model id of a dead provider cannot run elsewhere) and rejected throwing PlanInvalidError for the fallback/auditor collision (run-node's finally would dispose the tree and lose the work). Next worker must know: two pre-existing dead+resume tests (run-node.test.ts, phase-commit-retry.test.ts) encoded the superseded trigger and now pass a fallbackProvider — assertions unchanged; ENGINEERING.md's classify row and docs/journal.md's verdict row were corrected to match; README's `--fallback-provider` line is left for c6; and run-plan still journals `provider` from the plan, so a fallback attempt misattributes the casting join (G1) — untested, needs its own check.
- (GREEN) Tried: 2026-09-10 c3 green — bun run check exits 0 (584 pass, 0 fail), weeder --strict clean on the diff, evidence test 8/8. Implementation is run-node's dead branch re-casting on opts.fallbackProvider with auditDiversityRefusal re-asked against the resolved provider (refusal settles failed and keeps the tree; no usable fallback settles dead with verdictDetail), run-plan threading the option and relaying the reason as the verdict line's detail, and the CLI's --fallback-provider. Rejected carrying worker.model into a fallback spawn (a dead provider's model id cannot run elsewhere) and rejected throwing PlanInvalidError for the fallback/auditor collision (run-node's finally would dispose and lose the tree). Next worker must know: two pre-existing dead+resume tests now pass a fallbackProvider because D17 changed the trigger, not their subject; ENGINEERING.md's classify row and docs/journal.md's verdict row were corrected to match; README's --fallback-provider line is left for c6; and run-plan still attributes the verdict to node.worker.provider, so a fallback attempt misreports the casting join (G1) — untested, needs its own check.

#### nl.audit-verb (c4)
- (RED) Tried: 2026-09-10 wrote the RED e2e for c4 (`pleach audit` + the named egress expectation) with a single env-switched scripted config so run and re-audit meet different auditors in separate processes; proved "only the audit re-ran" with an out-of-worktree smoke/setup tally plus a builder stamp in app.txt rather than trusting prompts, and pinned `facts.base` as `{kind:'quarantine',sha}` for c5 to match; rejected three separate fixture configs (duplication) and a stateful in-process counter (the CLI invocations are separate processes anyway); confirmed RED — only these two tests fail, on the missing `expected` field and on `unknown verb 'audit'`.
- (IMPL) Tried: 2026-09-10 implemented c4 — `EXPECTED_EGRESS` on the unparseable line, `runAuditLadder` extracted from run-node so the verb and the run share one ladder, run-plan's settle helpers (`writeReceiptOrJournal`/`quarantineTree`/`disposeOrJournal`/`acceptanceOf`) lifted to exported functions rather than copied, `ReceiptFacts.base` sealed additively (CHANGES.md: Receipt, not schema), `pleach audit` wired in the CLI + README; rejected passing the quarantined delivery's file list to the SEC4a rule (it is recorded nowhere — a re-audit stages nothing, so instead the verb refuses any close that never reached an auditor, which is exactly the tamper-refused case) and rejected widening the green-smoke precondition to walk the receipt history, so a node re-quarantined by `pleach audit` still cannot be re-audited a second time — the refusal now says why, and closing it needs its own RED test.
- (GREEN) Tried: 2026-09-10 GREEN phase — verified rather than repaired: `bun run check` exit 0 (586 pass, 0 fail), `weeder check --strict` exit 0 with no findings, evidence test 2/2, and `git diff HEAD -- test/` empty so the red-phase test and its scripted-cast fixture are untouched; no new changes were needed, and the one recorded limitation (a node re-quarantined by `pleach audit` cannot be re-audited again — its own close records no smoke, so the literal latest-receipt precondition refuses it, legibly) is still open for a decision on whether to spend a node walking `previousReceiptSha256` back to the close that ran the smoke on the same `facts.base` tree.

#### nl.resume (c5)
- (RED) Tried: 2026-09-10 wrote test/loop/resume-quarantine.test.ts RED (9 tests, 5 failing on the missing resume, 4 guarding today's --fresh/no-quarantine behaviour incl. a byte-identical sealed-facts comparison); asserted baseRefs[0]=quarantine sha with deps merged after, facts.base on both the closed and the quarantined path plus rehash, the resumed-from-quarantine journal line, the full gate ladder on a provably-resumed run and a marker still failing it, and 'resuming work interrupted at <sha>' in the first prompt; rejected pinning the diff-stat format or a new isolate-seam method (not in the check, would fix a design the green phase should own), and rejected a phased-resume case here because the note's "HEAD carries the pleach-phase: red trailer" is wrong — quarantineTree commits on top of the red seal, so the trailer is on the parent — and c6's e2e owns phases; green must also add the journal.md row + KINDS entry for resumed-from-quarantine or journal-doc goes red, and should decide whether resume needs audit-node's C5 moved-ref refusal.
- (IMPL) Tried: 2026-09-10 implemented c5 — run-plan resolves `quarantine/<id>` for pending nodes and puts it at baseRefs[0] with deps merged, seals `facts.base` through settle/buildFacts on both the done and quarantine paths, journals `resumed-from-quarantine`, and added `--fresh` (CLI → RunPlanOpts) which leaves the sealed facts byte-identical to a run with no quarantine; the first prompt of any attempt that isolates carries `resuming work interrupted at <sha>: <stat>` via a new `IsolateSeam.commitStat` (`git show --stat --format=`, proven in test/integration/isolate.test.ts); added a superseded-quarantine refusal (`resume-refused`) because the branch outlives its close and a re-dispatched node would otherwise resurrect a failed tree its own verified close replaced; documented both events in docs/journal.md + KINDS, extended the `facts.base` note in CHANGES.md, and added the resume paragraph to README; grew the test with a two-run case (run 1 fails and quarantines, run 2 resumes from the real sha and stat) and the superseded case; rejected implementing phased resume — the briefed "HEAD carries the pleach-phase: red trailer" is wrong (the quarantine commit sits on top of the seal) and the seal message has no phase index, so `redSealedAt` can't be recovered; c6's e2e owns that, and it must also handle an aborted phased node whose clean tree quarantines nothing and loses its seals.
- (GREEN) Tried: 2026-09-10 GREEN — `bun run check` exits 0 (598 pass, 11 skip, 0 fail), weeder --strict clean, resume-quarantine 11/11; the only edit this phase corrected the `resumeFrom` comment, which claimed the resume skips ref verification while the code below it checks the node's latest receipt names that quarantine (the guard that stops a re-dispatched node resurrecting a failed tree its own verified close replaced — journaled as `resume-refused`); no behaviour changed and nothing was weakened; next worker: phased resume is still unbuilt and the briefed "HEAD carries the pleach-phase: red trailer" is wrong (the quarantine commit sits on top of the seal, and the seal message carries no phase index, so `redSealedAt` needs the index added there first), and an aborted phased node with a clean tree quarantines nothing and loses its seals — both belong to c6's e2e.

#### nl.docs (c6)
- (RED) Tried: 2026-09-10 Wrote the c6 RED in test/unit/journal-doc.test.ts — two blocks derived from source (artifact gates from run-plan's keepOrJournal call sites, kinds from the ArtifactKind union, filenames from ARTIFACT_EXTENSION, controls checked against the CLI's real dispatch/parse tables); 3 fail: the gate-artifact row lacks `handback` and `.handback.md`, and the README lacks `--fallback-provider`. Rejected asserting doc prose literally (rots) and requiring every parsed flag in the README (the README is not a manpage — it names controls, and the D16 block set that precedent). Next worker: the doc row's `gate` cell must parse as `` `gate` (`smoke`, `friction`, `handback`) `` for `statedVocabulary`, and each filename must appear inside backticks in that row.
- (IMPL) Tried: 2026-09-10 Made c6 green by documenting the `handback` gate + `.handback.md` in journal.md's gate-artifact row and adding a `--fallback-provider` paragraph to the README (verified against run-node.ts's dead-attempt block, not paraphrased from the help); rejected mentioning `policy.onDead` in the README since its default is `resume` and the README discusses no other policy field, and rejected touching src/ — every control c6 pins was already implemented, only the docs lagged. Next worker: the pin test derives the artifact gate list from `keepOrJournal` call sites in run-plan.ts and the filenames from `ARTIFACT_EXTENSION` in seams/receipts.ts, so a fourth artifact kind requires both the row's `gate (...)` vocabulary and its backticked suffix.
- (GREEN) Tried: 2026-09-10 GREEN phase was a no-op beyond verification — the implement-phase edits (journal.md's gate-artifact row gaining `handback`/`.handback.md`, README gaining the `--fallback-provider` paragraph) already carry `bun run check` to exit 0, and test/e2e/help.test.ts stays green; rejected touching src/ or the sealed pin test since every control c6 names is implemented and only the docs lagged. Next worker: the pin test derives artifact gates from `keepOrJournal` call sites in run-plan.ts and filenames from `ARTIFACT_EXTENSION` in seams/receipts.ts, so a fourth artifact kind needs both the row's `gate (...)` list and its backticked suffix.

#### nl.e2e (c7)
- (RED) Tried: 2026-09-10 wrote the sink e2e (test/e2e/nothing-is-lost.test.ts + test/fixtures/nothing-is-lost.config.ts) — one plan, real CLI + real git: a phased node SIGINTed after its red seal must resume from quarantine/<id> and close verified (facts.base, both hand-backs, both closes in the history), and `pleach audit` must re-adjudicate a garbage relay. RED for the right reason: today the resumed attempt re-enters the phase ladder at red, `sealRedPhase` commits over a clean tree and throws IsolateCatastrophicError, which escapes the ladder — the node fails "after 0 attempts" with no receipt and no quarantine, so that run's work is lost too. The empty-red guard misses it because collectDelivery trusts the worker's filesTouched over git. Rejected: faking the abort with a scripted `reason: 'aborted'` scenario (the real signal path is the claim) and duplicating audit-verb's counters here (that depth is pinned in test/e2e/audit-verb.test.ts; the sink asserts the outcome). Next worker: derive `redSealedAt` for a resumed tree (the `pleach-phase: red` trailer in its history) so the ladder re-enters after the seal rather than remaking one it cannot honestly produce.
- (IMPL) Tried: 2026-09-10 made c7 green by recording where a phased tree's ladder left off: receipt `facts.redSealedAt` (sealed, additive — CHANGES.md + journal.md updated), carried out on RunNodeResult, read back by run-plan's resumeFrom from the receipt that already gates the resume, seeded at every isolation, and passed through `pleach audit`'s close. Rejected deriving the index from git (a `pleach-phase` trailer walk) — the receipt is already the trust anchor for resuming and is already read there, so no new seam and no message parsing; rejected fixing `sealRedPhase`'s empty-seal crash here (collectDelivery trusts the worker's `filesTouched`, so a clean tree reaches `git commit` and throws IsolateCatastrophicError, failing the node with no receipt and no quarantine) — that guard is D13's and needs its own failing test, and it stays latent for any worker that names files it did not change. Added a RED-first refusal in test/loop/resume-quarantine.test.ts: an index that no longer names a red phase in the current plan refuses the seed, because without it a phase inserted ahead of the cycle was silently skipped and the node still closed verified. Note for the next worker: test/e2e/audit-verb.test.ts's re-quarantine test failed once in six full-suite runs under load and was not reproducible in ten more; it now asserts the verb's own stdout so the next occurrence says whether the verb refused or the child never ran.
- (GREEN) Tried: 2026-09-10 GREEN phase — `bun run check` exits 0 (608 pass, 0 fail; four consecutive runs) and `weeder check --strict` is clean; every check on the page passes against its own evidence file. Only edit this phase was re-wrapping the new CHANGES.md entry; the fix stands as implemented (receipt `facts.redSealedAt` carried close→resume→re-adjudication, plus the `resume-refused` guard when the plan's phase list no longer names that index a red). Next worker should know: one `bun run check` stalled >10 min mid-suite under heavy machine load and I killed the runner — the live child was a config-less `pleach run` (teardown/quarantine/receipt family, not this node's test) and no per-test timeout fired, which reads as process starvation rather than a wedged test; it did not recur in four runs and no orphan processes or tmp repos are left behind. Still latent from the RED phase: `sealRedPhase` reaches `git commit` over a clean tree when a worker overreports `filesTouched`, and that crash costs the run its receipt and quarantine — D13's guard, needing its own failing test.
